# RESUME_POINT

## Текущая точка продолжения (актуально)

- [2026-04-12 19:20:00] Первый живой user-facing результат `packet-up-safe` уже положительный: `YouTube` home/feed и `Instagram` ведут себя лучше, чем на `stream-one-safe`. Значит, `packet-up-safe` становится текущим лидирующим кандидатом внутри XHTTP-линейки по browsing/UI latency.
- [2026-04-12 19:20:00] Ближайшая практическая задача теперь уже точечная: добрать по `packet-up-safe` ещё два критерия — `first ping / cold-start` и `Telegram/Discord calls`. Если они не просядут, этот preset можно будет фиксировать как лучший текущий XHTTP-кандидат для Block C1.
- [2026-04-12 18:54:00] Staging-шаг для `packet-up-safe` уже закрыт: preset установлен на тестовую ноду, strict `verify` и `acceptance` завершились полным `PASS`. Это значит, что ближайшая задача теперь снова user-facing, а не инфраструктурная.
- [2026-04-12 18:54:00] Практический следующий шаг теперь конкретный: сравнить `packet-up-safe` и `stream-one-safe` по `first ping`, скорости открытия лент, `YouTube` UI, обычному browsing и `Telegram/Discord` calls. Если `packet-up-safe` не даст заметного выигрыша, это будет сильным признаком, что мы упёрлись не в частный preset-gap, а в предел самого `XHTTP`-профиля для `cold-start`.
- [2026-04-12 16:05:00] Новый полевой результат по `stealth-xhttp stream-one-safe`: профиль остаётся рабочим и стабильным для browsing и voice/video, но `cold-start` по-прежнему держится на уровне около `6` секунд. Это важное уточнение: realtime-стабильность уже не главный незакрытый хвост, а остаточная latency-проблема первого установления канала.
- [2026-04-12 16:05:00] Значит, следующий safe-slice нужно вести уже не через очередное ужесточение keepalive/user-timeout, а через другой совместимый `XHTTP mode`, сохраняя текущий консервативный batching и выключенный `XMUX`, чтобы не потерять достигнутую стабильность.
- [2026-04-12 16:12:00] Для этого подготовлен новый preset `packet-up-safe`: он изолирует ровно один фактор относительно `stream-one-safe` — `XHTTP mode=packet-up`. Ближайший практический шаг теперь конкретный: staging `install -> verify -> acceptance`, затем user-facing сравнение `packet-up-safe` против `stream-one-safe` по `first ping`, лентам, UI-запросам и звонкам.
- [2026-04-12 04:20:00] Новый полевой результат по `stealth-xhttp realtime-safe`: профиль в целом работает хорошо и не выглядит проваленным для реального использования, но остаётся отдельный симптом `cold-start / small-request latency` — первое установление соединения и первый ping могут занимать около `5` секунд, а мелкие API/ленточные запросы (`Instagram feed`, часть `YouTube` UI-запросов) ощущаются тяжелее, чем media-content.
- [2026-04-12 04:20:00] Это важное уточнение: проблема сейчас выглядит не как плохая скорость канала в целом и не как сломанный handoff, а как отдельный latency-profile `XHTTP` на мелких запросах. Значит, следующий safe-slice нужно вести по линии `cold-start/API latency`, а не возвращаться к уже закрытому handoff-расследованию.
- [2026-04-12 04:20:00] Практический следующий шаг: сохранить `realtime-safe` как подтверждённый рабочий preset, затем открыть новый controlled speed/latency slice для `XHTTP` и сравнить `first ping`, `Instagram feed`, `YouTube thumbnails/feed`, обычный browsing и realtime-calls на тех же устройствах.
- [2026-04-12 04:28:00] Этот следующий slice уже подготовлен кодом как `api-latency-safe`: он остаётся в `stream-up`, не включает `XMUX`, но ещё сильнее уменьшает batching и чуть жёстче ограничивает keepalive/user-timeout, чтобы проверить именно latency мелких запросов и холодный старт канала.
- [2026-04-12 04:28:00] Ближайший практический шаг теперь конкретный: выложить `api-latency-safe` на staging, прогнать `verify/acceptance` и только потом сравнивать его с `realtime-safe`. Базовый `realtime-safe` при этом не заменяется и остаётся текущей рабочей точкой.
- [2026-04-12 15:05:00] Этот staging-шаг уже закрыт: `api-latency-safe` установлен на тестовую ноду, strict `verify` и `acceptance` завершились полным `PASS`. Значит, текущая ближайшая задача теперь уже user-facing: сравнить `api-latency-safe` и `realtime-safe` по `first ping`, `Instagram feed`, `YouTube UI/feed`, обычному browsing и `Telegram calls`.
- [2026-04-12 15:15:00] Новый полевой вывод уточнил картину: `api-latency-safe` даёт только небольшой выигрыш, но не снимает сам класс проблемы. Поэтому следующий safe-slice уже не про более мелкий batching в `stream-up`, а про другой XHTTP mode.
- [2026-04-12 15:15:00] Для этого подготовлен preset `stream-one-safe`: он сохраняет выключенный `XMUX` и консервативные TCP-параметры, но переключает `XHTTP mode` в `stream-one`. Ближайший шаг теперь конкретный: staging `verify/acceptance`, затем user-facing сравнение `stream-one-safe` против `api-latency-safe` и `realtime-safe`.
- [2026-04-12 15:17:00] Этот staging-шаг уже закрыт: `stream-one-safe` установлен на тестовую ноду, strict `verify` и `acceptance` завершились полным `PASS`. Значит, текущая ближайшая задача теперь чисто пользовательская: сравнить `stream-one-safe` против `api-latency-safe` и `realtime-safe` по `first ping`, лентам, UI-запросам и звонкам.
- [2026-04-12 01:25:00] Новый живой вывод по `stealth-xhttp`: текущий handoff/browsing baseline уже хороший, но видеозвонки `Discord` и `Telegram` остаются тяжёлым realtime-сценарием. Следующий safe-slice теперь не абстрактное «ускорение XHTTP», а отдельный controlled preset `realtime-safe` именно под uplink/latency-чувствительную нагрузку.
- [2026-04-12 01:25:00] `realtime-safe` уже добавлен в код как opt-in preset: он принудительно использует `mode=stream-up`, не включает `XMUX` и держит короткий TCP keepalive/timeout-контур. Цель — проверить, можно ли улучшить voice/video calls без деградации `Wi-Fi <-> LTE` handoff и без возврата старых длинных зависаний.
- [2026-04-12 01:25:00] Если этот preset не даст заметного улучшения, вывод будет продуктовый, а не только tuning-level: `stealth-xhttp` остаётся хорошим stealth/browsing профилем, но не претендует на роль лучшего realtime-call профиля. Тогда выбор основного low-latency режима смещается к `stealth-xray`, а будущий compatibility-класс должен учитывать отдельный call-friendly fallback.
- [2026-04-12 00:55:00] Транспортный roadmap уточнён: текущее anti-DPI ядро по-прежнему ограничено `stealth-xray` и `stealth-xhttp`, а `Trojan gRPC`, `WS TLS` и другие дополнительные подключения не забыты, но переводятся в отдельный compatibility/fallback-класс после стабилизации ядра.
- [2026-04-12 00:55:00] Правило unified baseline тоже зафиксировано явно: конечный основной контур будет собираться только из подтверждённых профилей, а не из всех доступных transport-схем сразу. Целевая модель — `primary low-latency`, `primary stealth`, `compatibility fallback`.
- [2026-04-12 00:55:00] Перед открытием каждого следующего transport-модуля действует тот же admission-контур: `install`, `verify`, `acceptance`, `backup/restore`, клиентский import/update subscription и живой полевой тест. Это нужно, чтобы baseline расширялся управляемо, а не превращался в непроверенный набор разнородных профилей.
- [2026-04-12 00:20:00] Полевой результат `stealth-xhttp handoff-safe` подтверждён пользователем: `Wi-Fi <-> LTE` восстанавливается обычно мгновенно или за `3-5` секунд, без прежних пауз `30-60` секунд; профиль работает на Android, ПК и Shadowrocket/iPhone.
- [2026-04-12 00:20:00] `handoff-safe` фиксируется как текущий успешный XHTTP baseline и не должен перетираться экспериментами. Следующий speed-срез открывается отдельным opt-in preset `balanced-speed`, чтобы ускорять XHTTP без потери уже подтверждённого handoff-контракта.
- [2026-04-12 00:20:00] Ближайшая практическая проверка после выкладки `balanced-speed`: сравнить его с `handoff-safe` на тех же сценариях `Wi-Fi -> LTE`, `LTE -> Wi-Fi`, YouTube video/thumbnails, обычный web, Telegram/мессенджеры и Shadowrocket/iPhone.
- [2026-04-11 21:05:00] Следующий test-slice: выложить `stealth-xhttp` с новым `XHTTP_TUNING_PROFILE=handoff-safe`, заново импортировать JSON-подписку в `v2rayNG` и измерить именно направление `LTE -> Wi‑Fi`; целевой критерий — восстановление заметно быстрее прежних 30-60 секунд без ручного reconnect.
- [2026-04-11 21:05:00] Если `handoff-safe` не улучшит `LTE -> Wi‑Fi`, проблема считается не XMUX/preset-level, а client/network-level ограничением `XHTTP/H2`; тогда `XHTTP` не должен становиться primary-профилем, а primary-кандидатом остаётся `stealth-xray`.
- [2026-04-11 20:45:00] Следующий delivery/tuning слой для `stealth-xhttp` закрыт кодом: JSON-подписка теперь должна приносить клиенту и валидный `vnext`, и `streamSettings.sockopt`; кроме того, `low-latency` больше не держит фиксированный долгоживущий `XMUX`, который мог мешать быстрому handoff.
- [2026-04-11 20:45:00] Ближайший практический шаг после выкладки на staging: переустановить `stealth-xhttp low-latency`, заново импортировать JSON-подписку и проверить два симптома отдельно — скорость обычного мобильного browsing и длительность `Wi‑Fi <-> LTE` восстановления.
- [2026-04-11 20:45:00] Если handoff заметно сокращается, этот фикс оставляем как новый baseline для `stealth-xhttp`; если нет, следующий шаг уже не в JSON delivery, а в сравнении `stealth-xhttp` против `stealth-xray` как основного/резервного профиля.
- [2026-04-11 20:05:00] Новый критичный user-facing баг закрыт кодом и staging-переустановкой: публичный `XRAY JSON Subscription` больше не отдаёт сломанные `vless`-конфиги upstream `3x-ui`; JSON import теперь должен проверяться только после удаления старых `null:null` профилей и повторного импорта с нового endpoint.
- [2026-04-11 20:05:00] Ближайший практический шаг: попросить пользователя заново импортировать JSON-подписку (`v2rayNG install-config` / update subscription), подтвердить, что профили больше не отображаются как `null:null`, а затем снова вернуться к живому handoff-тесту `Wi‑Fi <-> LTE` для `stealth-xhttp low-latency`.
- [2026-04-11 20:05:00] Если после повторного импорта JSON-путь оживает, следующий незакрытый вопрос уже не в формате подписки, а в реальной transport-устойчивости: сколько длится handoff/reconnect, нужен ли ручной reconnect и как `stealth-xhttp` сравнивается с `stealth-xray` на живой сети.
- [2026-04-11 19:40:00] Новый low-latency шаг для `stealth-xhttp` закрыт кодом и staging-проверкой: `xhttpSettings.xmux` теперь реально доставляется в inbound и в JSON subscription, а `verify` и loopback self-test валидируют этот же контракт.
- [2026-04-11 19:40:00] Следующий практический шаг уже user-facing: попросить ручной `Wi‑Fi <-> LTE` handoff-тест именно через `XRAY JSON Subscription` / `v2rayNG install-config`, потому что прямой QR/direct URI по-прежнему не переносит `xmux`.
- [2026-04-11 19:40:00] Ближайшая цель triage не меняется: понять, сокращает ли этот `xmux`-слой handoff-задержку `stealth-xhttp`; если нет, следующим кандидатом будет уже не transport-delivery, а `H2/H3`-класс ограничения и дальнейшее runtime-разделение.
- [2026-04-11 19:35:00] Новый product-bug закрыт кодом: JSON subscription delivery в `3x-ui` больше не должен silently ломаться из-за отсутствующего `subJsonEnable`; `verify` теперь отдельно проверяет `https://<domain>/<subJsonPath>/first`.
- [2026-04-11 19:35:00] Следующий практический шаг после выкладки этого фикса на staging: подтвердить, что JSON-sub path реально ожил, затем уже решать второй слой проблемы `XHTTP` — handoff-задержку `Wi‑Fi <-> LTE`, которая сейчас выглядит как H2/XMUX/client-side reuse-поведение, а не как прежний server-side баг.
- [2026-04-11 18:25:00] Новый triage-вывод подтверждён жёстко: `stealth-xhttp` был сломан не из-за пользователя, не из-за мобильной сети и не из-за самого `nginx grpc_pass`, а из-за server-side `xPaddingBytes` в `x-ui`-generated inbound.
- [2026-04-11 18:25:00] Следующий практический шаг после этого патча один: обновить staging-ноду до свежего `codex/platform-v2`, повторить `install + verify` для `stealth-xhttp` и только после успешного self-test возвращаться к ручному сравнению `stealth-xray` vs `stealth-xhttp`.
- [2026-04-11 18:40:00] Отдельный backlog после закрытия текущего transport-блока: усилить `site realism` для обоих доменов, чтобы active probing видел не просто рабочий fallback, а правдоподобные живые HTTPS-сайты на каждой публичной поверхности.

- [2026-04-10 22:10:47] Канонический этап в работе: `XRAY_DPI_PLAN.md`, блок `C1` (must-have) — невидимость для DPI + long-running/reconnect устойчивость + приемлемая скорость.
- [2026-04-10 22:10:47] Активный staging: `217.199.253.102`; оба stealth-профиля используют `mobile-safe` слой для controlled сравнения.
- [2026-04-10 22:10:47] Ближайший практический шаг: полевой сравнительный прогон `stealth-xray` vs `stealth-xhttp` по нескольким сетям/операторам с фиксацией инцидентов по единому шаблону.
- [2026-04-10 22:48:10] Технологический контракт текущего этапа вынесен в `PROTOCOL_HARDENING_PLAN.md`: `RST`-защита = снижение детекта + resilience/fallback, а не отдельная "волшебная" firewall-команда.
- [2026-04-10 22:53:58] Новый важный риск для triage: `single-IP co-tenancy` — одновременная работа `Xray` и дефолтного `AWG` на одном IP может провоцировать общую деградацию всего IP, поэтому `AWG` сначала держим как резервный/отдельный контур.
- [2026-04-10 23:00:59] Уточнение по `AWG`: hardened `AWG` не запрещён для будущего основного IP, но сначала должен пройти отдельную реализацию и co-tenancy acceptance; дефолтный контейнерный запуск не подходит как production-сосед `Xray`.
- [2026-04-10 23:05:05] Новый симптом для текущего triage: `Reality First` работает на Wi‑Fi, но на мобильной сети даёт `I/O read/write on closed pipe`; текущая гипотеза — mobile whitelist/IP-level drop, поэтому нужен отдельный whitelist-resilient fallback-класс.
- [2026-04-10 23:09:41] Итоговая идея форка и staged-план работ вынесены в `PLATFORM_VISION_PLAN.md`; HTML-версия для ветки находится в `docs/branch-html/codex-platform-v2-strategy.html`.
- [2026-04-10 23:30:48] Visual docs-слой пересобран: `docs/branch-html/index.html` и все branch-specific HTML теперь на русском, с подробными карточками назначения, install-режима, рисков, проверок, стоп-условий и дальнейших шагов; генератор находится в `docs/branch-html/render-branch-html.ps1`.
- [2026-04-11 00:05:00] Новый положительный полевой результат: текущий тестовый контур `platform-v2` у пользователя переживает ручное переключение `Wi‑Fi <-> mobile` без заметной длинной паузы на восстановление; старый симптом `~50 секунд до возврата интернета`, наблюдавшийся на прежнем `xhttp`-контуре, в этом тесте не повторился.
- [2026-04-11 00:05:00] Ближайший практический шаг теперь уже не «искать хоть какой-то рабочий handoff», а подтвердить его повторяемость на длинной сессии и понять, остаётся ли результат стабильным при разном времени суток и при последующем `mobile whitelist` окне.
- [2026-04-11 10:28:01] Новый продуктовый инвариант зафиксирован явно: server-side эволюция платформы должна по возможности сохранять совместимость подписки; обычный `subscription refresh` допустим, а полный re-import и ручная перенастройка клиентов считаются крайним исключением, а не нормой.
- [2026-04-11 10:28:01] Транспортный фокус тоже зафиксирован явно: сейчас не открываем новые экзотические transport-ветки, а закрываем `C1` только на `stealth-xray` и `stealth-xhttp`; `ICMP` не входит в roadmap для `VLESS + REALITY`, а compatibility-пути (`Trojan gRPC`, `WS TLS` и близкие схемы) возвращаются в работу только как отдельный fallback-класс после основной приёмки.
- [2026-04-11 11:11:25] Следующий отложенный backlog после `hardened-awg` тоже уже зафиксирован: отдельно исследовать `Hysteria` как будущий transport/provider-модуль и `MTProto Telegram` как отдельный сервисный модуль для узкой задачи доступа к Telegram; до этого этапа их не подмешиваем в активную реализацию.
- [2026-04-11 14:40:00] Текущий кодовый шаг закрыт: preset-layer для `REALITY`/`XHTTP` теперь можно выбирать через `-reality_tuning_profile` и `-xhttp_tuning_profile`, а `stage=acceptance` сохраняет `runtime-snapshot.env` и redacted summary по inbound'ам для comparative triage.
- [2026-04-11 15:20:00] Следующий operational-safe шаг тоже закрыт: install-контур сохраняет `/etc/x-ui/runtime-provenance.env`, а `backup/restore` теперь умеет переносить и читать этот sidecar-файл, не теряя точный `transport/tuning` контекст после восстановления.
- [2026-04-11 16:05:00] Следующий кодовый шаг для блока `C1` тоже закрыт: comparative acceptance теперь имеет machine-readable matrix-слой (`session-metadata.env`, `matrix-row.json`, `probe-results.jsonl`) и поддерживает явные метки сети, оператора, времени суток и клиента.
- [2026-04-11 14:40:00] Следующий ближайший практический шаг: живой сравнительный прогон `stealth-xray` vs `stealth-xhttp` уже не только на `mobile-safe`, но и на новых controlled preset'ах `low-latency` / `aggressive-stealth` с фиксацией профиля, времени суток, handoff и повторяемости.
- [2026-04-10 22:10:47] После закрытия `C1`: формальная production-рекомендация «основной профиль + fallback-профиль», затем переход к `stealth-multi`/`AWG`.
- [2026-04-10 22:10:47] Документация приведена к единому процессу через `DOCS_WORKFLOW.md`; текущий ориентир по docs-слою — `INDEX.md` и `DOCS_MATRIX.md`.

## История уточнений

- [2026-04-10 22:10:47] Ниже сохранён исторический журнал уточнений; старые записи могут содержать уже неактуальные staging-домены и промежуточные гипотезы.

- [2026-04-08 23:24:11] Текущая точка продолжения снова смещена: второй stealth transport `stealth-xhttp` уже открыт, установлен на staging и проходит строгий `verify`.
- [2026-04-08 23:24:11] Autodetect нового профиля тоже подтверждён: повторный `-stage verify` без явных env-переменных сам восстанавливает `stealth/stealth-xhttp` из `x-ui.db`.
- [2026-04-08 23:24:11] Канонический рабочий документ текущего этапа остаётся `docs/XRAY_DPI_PLAN.md`, но активный фокус внутри него теперь смещается с блока B на блок C: anti-DPI матрица приёмки и user-facing проверки.
- [2026-04-08 23:24:11] Переход к `AWG` по-прежнему отложен; ближайшая задача — не новый transport, а формализация критериев выбора и проверки двух runnable stealth-путей.
- [2026-04-09 00:15:49] Эта формализация уже зафиксирована в отдельном документе `docs/ANTI_DPI_MATRIX.md`.
- [2026-04-09 00:30:57] Следующая практическая точка продолжения теперь ещё конкретнее: server-side comparative helper уже реализован как `-stage acceptance` и подтверждён на staging для обоих stealth-профилей.
- [2026-04-09 00:30:57] Значит, следующий незакрытый шаг — уже не кодовый transport-срез, а живая клиентская приёмка через `v2rayN` и длинные сессии 30-60 минут по матрице `ANTI_DPI_MATRIX.md`.
- [2026-04-09 01:10:14] Первый клиентский тест уже получен: текущий `stealth-xray` выдержал около 10 минут нагрузки `4K`-видео и параллельного браузерного трафика без замеченных обрывов.
- [2026-04-09 01:32:44] Симметричный клиентский тест тоже уже закрыт: текущий `stealth-xhttp` выдержал около 10 минут нагрузки `4K`-видео и параллельного браузерного трафика без замеченных обрывов.
- [2026-04-09 01:32:44] Следующая точка продолжения теперь смещается на длинные сессии `30-60` минут и reconnect-проверки для обоих stealth-профилей; только после этого можно собирать итоговую рекомендацию выбора режима.
- [2026-04-09 20:50:25] Следующая точка продолжения снова уточнена по живому полевому отчёту: коротких client-load тестов недостаточно, потому что на mobile internet старые reality-схемы фактически не подтверждены, а текущий `stealth-xhttp` деградирует в длительном дневном использовании.
- [2026-04-09 20:50:25] Значит, ближайший реальный шаг — не новый transport и не `AWG`, а `mobile-data acceptance + time-of-day degradation investigation`: воспроизвести проблему, собрать логи по времени суток, сравнить `stealth-xray` и `stealth-xhttp` в одной и той же мобильной сети и только потом собирать итоговую рекомендацию.
- [2026-04-10 00:34:06] Подготовительный кодовый слой для этого шага уже закрыт: `xhttp`-tuning переведён в transport metadata и больше не прибит к SQL-литералам в двух местах installer'а.
- [2026-04-10 00:34:06] Ближайший следующий шаг теперь конкретный: ввести первый `mobile-safe` tuning-профиль для `stealth-xhttp`, усилить transport-level acceptance и только потом снова идти в длительные mobile-data прогоны.
- [2026-04-10 08:06:43] Усиление acceptance тоже уже сделано: `stage=acceptance` теперь сохраняет `ss -tinp`, `ss -s`, `journalctl` и transport-aware handoff по `xhttp` и `reality`, поэтому следующий mobile triage можно вести уже по нормальным артефактам.
- [2026-04-10 08:06:43] Ближайший следующий шаг теперь уточняется так: сначала собрать первый `mobile-safe` preset для `stealth-xhttp`, затем отдельно подготовить `mobile-safe` preset для `stealth-xray/reality-shield`, и только после этого возвращаться к долгим mobile-data проверкам.
- [2026-04-10 08:10:51] Первый такой preset уже собран: текущий `stealth-xhttp` в ветке `codex/platform-v2` переключён на `mobile-safe`.
- [2026-04-10 08:10:51] Ближайший практический шаг теперь двойной: загрузить этот `mobile-safe stealth-xhttp` на staging для полевого теста, а затем подготовить симметричный controlled preset для `stealth-xray/reality-shield`.
- [2026-04-10 08:14:46] Этот staging-шаг уже закрыт: `mobile-safe stealth-xhttp` установлен на `185.207.64.40`, `verify` проходит, поэтому пользователь уже может тестировать именно новый preset, а не предыдущий default.
- [2026-04-10 08:14:46] Следующий ближайший шаг теперь смещается на две параллельные цели: полевой mobile-data тест нового `stealth-xhttp` и подготовка симметричного controlled preset для `stealth-xray/reality-shield`.
- [2026-04-10 19:41:17] По фактическому состоянию инфраструктуры точка продолжения обновлена: старый staging был недоступен из-за провайдерного инцидента, поэтому рабочий контур перенесён на новый сервер `217.199.253.102`.
- [2026-04-10 19:41:17] На новом сервере подтверждён рабочий baseline `stealth/stealth-xhttp` (install + strict `verify` + `acceptance`), значит деградацию mobile-data дальше нужно расследовать уже от чистой точки, а не от аварийного состояния прошлой ноды.
- [2026-04-10 19:41:17] Симметричный `mobile-safe` шаг для `REALITY` уже выполнен в коде: `stealth-xray` и `stealth-xhttp` переведены на `TRANSPORT_REALITY_TUNING_PROFILE=mobile-safe` и fingerprint `chrome`; следующий практический шаг — полевые тесты `stealth-xray` против `stealth-xhttp` на новой ноде.
- [2026-04-10 20:07:20] Отдельно зафиксирован продуктовый слой требований «всегда на связи» в `docs/PROJECT_CONNECTIVITY_REQUIREMENTS.md`; этот файл теперь обязателен при принятии решений по Block C и следующему шагу с `AWG`.
- [2026-04-10 20:07:20] Ближайшая цель после закрытия Block C уточнена: внедрить `stealth-multi` на основном сервере и поднять `AWG` сначала только на резервном сервере, чтобы не рисковать основным каналом связи.
- [2026-04-10 22:10:47] Точка продолжения уточнена как `Block C1` внутри `XRAY_DPI_PLAN.md`: must-have критерий теперь формулируется как «невидимость для DPI + устойчивость long-running/reconnect + приемлемая скорость», а не просто короткий успешный тест.
- [2026-04-10 22:10:47] Что нужно закрыть перед следующим архитектурным шагом: сравнительный mobile-data прогон `stealth-xray` и `stealth-xhttp` по нескольким сетям/операторам, фиксация инцидентов по единому шаблону и формальная production-рекомендация основного + fallback stealth-профиля.
- [2026-04-08 23:00:27] Текущая точка продолжения снова уточнена: baseline `stealth/stealth-xray` уже доведён до полного operational parity на staging и больше не является ближайшим незавершённым хвостом.
- [2026-04-08 23:00:27] Канонический рабочий документ текущего этапа остаётся `docs/XRAY_DPI_PLAN.md`, но активный фокус внутри него смещается с блока A на блок B: следующий шаг — второй stealth transport.
- [2026-04-08 23:00:27] Переход к `AWG` по-прежнему отложен до формального закрытия всего `Xray/DPI` этапа.
- [2026-04-08 23:00:27] Идеи по ускорениям и performance-оптимизации не потеряны: они остаются отдельным будущим backlog-блоком после закрытия текущего этапа.
- [2026-04-08 04:30:47] Перед большим платформенным обновлением начат отдельный организационный этап: в `docs/` введён канонический слой поверх накопившихся исторических файлов.
- [2026-04-08 04:30:47] Для повседневной работы теперь нужно ориентироваться прежде всего на `PROJECT_STATE.md`, `OPERATIONS.md`, `BRANCH_MATRIX.md`, `DOCS_MATRIX.md`, `MASTER_PLAN.md`, `FORK_PASSPORT.md` и `PLATFORM_BLUEPRINT.md`.
- [2026-04-08 04:30:47] Старые документы не удалены, но переведены в роли вспомогательных или исторических источников; новые этапы не должны снова расползаться по нескольким конкурирующим файлам.
- [2026-04-08 04:30:47] Порядок по веткам тоже формализован: `main` остаётся стабильным baseline, `codex/staging-reset-workflow` и `codex/operational-regression` считаются историей, а `codex/stealth-awg-architecture` остаётся текущей design-веткой до открытия новой платформенной implementation-ветки.
- [2026-04-08 04:55:17] Следующий организационный шаг уже закрыт: новая ветка `codex/platform-v2` открыта от `main`, а не от переходных `codex/*`.
- [2026-04-08 04:55:17] В `codex/platform-v2` уже перенесён безопасный подготовительный слой из `legacy-audit` и `stealth-awg-architecture`: archive-guard для `x-ui-pro-old.sh`, cleanup active installer-path и актуальный слой `docs/`.
- [2026-04-08 05:09:48] В `codex/platform-v2` уже выполнен первый code-slice новой платформы: появился минимальный модульный каркас `core/platform-lib.sh`, `modules/profiles/classic`, `modules/transports/classic-xray`, `providers/panels/3x-ui`.
- [2026-04-08 05:09:48] `x-ui-pro-updated.sh` теперь знает о platform selection (`PLATFORM_PROFILE`, `TRANSPORT_PROFILE`, `PANEL_PROVIDER`, `ENABLE_AWG`) и пишет её в debug/dry-run контекст, но фактическое поведение install-контура `classic` пока не изменено.
- [2026-04-08 05:09:48] Локальные проверки уже подтверждают, что новый слой синтаксически валиден, корректно инициализирует baseline-комбинацию и защитно режет ещё не реализованный `ENABLE_AWG=yes`.
- [2026-04-08 05:34:52] Следующий безопасный срез уже тоже выполнен: ключевые orchestration decision points installer'а переведены на selection-layer, а `main()` больше не вызывает напрямую `setup_nginx`, `enable_nginx_sites`, `install_panel`, `update_xui_db` и прямое управление `x-ui`.
- [2026-04-08 05:34:52] Provider metadata теперь уже участвует в runtime-слое `classic`: `show_details()` и orchestration используют не жёстко зашитые строки, а значения provider metadata `X-UI Secure Panel / x-ui / x-ui`.
- [2026-04-05 05:58:19] Завершена первая фаза локализации repo-owned зависимостей внутри форка.
- [2026-04-05 05:58:19] `vendor/` уже встроен в поддерживаемый контур установщика.
- [2026-04-05 05:58:19] `sub-3x-ui.html`, `sub-3x-ui-classical.html`, `clash/*.yaml`, `vendor/sb-rule-sets/*.json` уже переведены на локальные пути форка.
- [2026-04-05 05:58:19] `vendor/rule-assets/` уже содержит зеркала 52 upstream-файлов, которые раньше подтягивались внешне через шаблоны `sing-box`.
- [2026-04-05 05:58:19] `randomfakehtml.sh` уже работает по локальному набору шаблонов и исключает два шаблона с авто-подгрузкой внешних ресурсов.
- [2026-04-05 06:17:58] В `x-ui-pro-updated.sh` встроен первичный debug-mode с `-stage verify`, `-stage websub`, `-debug yes`, `-dry_run yes`, `-verify yes`, `-keep_artifacts yes`.
- [2026-04-05 06:17:58] Legacy-скрипт `x-ui-pro.sh` теперь безопасно передаёт diagnostic/stage-флаги в `x-ui-pro-updated.sh` до выполнения своей очистки.
- [2026-04-05 06:17:58] Исправлены два важных дефекта install-логики: чтение старой `x-ui.db` теперь происходит до очистки, а `REALITY` public key берётся из корректного поля `PublicKey`.
- [2026-04-05 06:56:44] Реальный full smoke-test уже выполнен на staging VPS `185.207.64.40` с `-auto_domain yes`; после установки `-stage verify` проходит полностью.
- [2026-04-05 06:56:44] Дополнительно закрыты ещё четыре практических дефекта: `CRLF`-несовместимость shell-скриптов на Linux, слишком жёсткий QEMU preflight fail, ложное переключение `-verify yes` в `stage=verify`, некорректный `EXIT trap`.
- [2026-04-05 07:01:41] Локальный репозиторий уже переведён на рабочую git-схему: `origin = RM-COM/AT-VPN-System`, `upstream = mozaroc/x-ui-pro`, текущая базовая ветка — `main`.
- [2026-04-05 07:03:16] Отдельный сценарий `-stage websub -verify yes -debug yes` уже повторно прогнан на установленной staging-ноде и завершился успешно с полным набором `PASS`.
- [2026-04-05 07:04:56] Первый базовый commit форка уже опубликован в `origin/main`, поэтому GitHub-репозиторий инициализирован и готов к дальнейшей работе.
- [2026-04-05 07:06:52] Зафиксировано новое системное правило проекта: основной цикл разработки теперь формализован как `Windows -> GitHub -> Linux staging`, а staging должен уметь возвращаться в чистое состояние перед новым полным прогоном.
- [2026-04-05 07:06:52] Сценарии полного удаления и восстановления признаны обязательной частью форка; их нужно сохранять и включать в дальнейшие проверки при рефакторинге.
- [2026-04-05 07:13:07] Подробный главный план следующего этапа теперь зафиксирован в `MASTER_PLAN.md`; это основной документ, от которого дальше ведётся работа.
- [2026-04-05 07:23:54] `README.md` очищен от следов чужого форка и приведён к актуальному состоянию именно `AT-VPN-System`; теперь он описывает только наш репозиторий, наш install/debug workflow и наши поддерживаемые сценарии.
- [2026-04-05 07:23:54] В правила проекта добавлено обязательное завершение каждого логического этапа через `commit + push` в `origin`; это становится частью основного рабочего контура.
- [2026-04-05 07:35:53] В `x-ui-pro-updated.sh` реализован новый `stage=reset`: staging-ноду теперь можно очищать через отдельный защитный reset-поток с `-confirm_reset yes`, pre-reset артефактами и post-check чистого состояния.
- [2026-04-05 07:35:53] Полный uninstall-контур усилен: вместе с `x-ui` и `nginx` теперь удаляются остатки `sub2sing-box`, `web-sub`, `certbot` и каталоги сертификатов.
- [2026-04-05 07:43:17] Дополнительно исправлен runtime-вывод `stage=reset`: новые сообщения reset-сценария переведены в ASCII English, чтобы staging-консоль не показывала битую кириллицу.
- [2026-04-05 07:49:59] Реальный `stage=reset` на staging VPS уже пройден успешно с полным `PASS`.
- [2026-04-05 07:49:59] Следующий clean install после reset выявил реальный `nginx`-баг порядка `load_module`; в код уже внесён фикс через `modules-enabled`, но повторный runtime-прогон install после этого ещё впереди.
- [2026-04-05 08:02:47] Повторный clean install после фикса `nginx`-модулей подтверждён фактическим состоянием сервера: `nginx`, `x-ui`, `sub2sing-box` активны, `x-ui.db` и `/var/www/subpage` на месте, `nginx -t` проходит.
- [2026-04-05 08:02:47] Отдельные `stage=verify` и `stage=websub` после этого clean install также уже повторно завершились с полным `PASS`.
- [2026-04-06 03:10:17] На ветке `codex/operational-regression` уже выполнен отдельный runtime-аудит operational-сценариев: `backup` создаёт полный архив install-контура, `x-ui-pro.sh -uninstall yes` очищает систему через актуальный uninstall-контур, а `restore` возвращает сервисы в рабочее состояние.
- [2026-04-06 03:10:17] Финальный staging regression `backup -> uninstall -> restore -> stage=verify` завершён с полным `PASS`; backup каталога `/backup/2026-04-06/00-08-08` достаточно для восстановления панели, `nginx`, сертификатов, web-root'ов и `sub2sing-box`.
- [2026-04-06 04:15:15] `sub2sing-box` в поддерживаемом контуре уже переведён на `systemd`-модель: `stage=verify` видит активный `sub2sing-box service`, uninstall/reset удаляют unit-файл, а новый backup `/backup/2026-04-06/01-13-17` уже включает `sub2sing-box.service`.
- [2026-04-06 04:15:15] `x-ui-pro.sh -install yes` теперь тоже уходит в `x-ui-pro-updated.sh`, поэтому поддерживаемая установка и поддерживаемая диагностика/очистка используют один и тот же современный кодовый путь.
- [2026-04-06 04:15:15] Clean install retest через `x-ui-pro.sh -install yes` дошёл до `certbot`, но упёрся во внешний exact-set rate limit Let's Encrypt; для тех же auto-domain повтор возможен после `2026-04-06 13:20:21 UTC` (`2026-04-06 16:20:21` по Москве) либо с другим набором доменов.
- [2026-04-06 04:34:59] Для обхода внешнего rate limit повторный clean install и repeated install уже прогнаны на staging с доменами `185.207.64.40.sslip.io` и `185-207-64-40.sslip.io`; оба install-потока завершились успешно, а `stage=verify` после каждого прохода вернул полный `PASS`.
- [2026-04-06 04:34:59] В installer уже зафиксирован reuse сертификатов: при повторной установке на те же домены `obtain_ssl()` переиспользует существующие сертификаты и не тратит новый лимит `certbot`.
- [2026-04-06 04:34:59] Cleanup repeated install уже исправлен: базовый layout `nginx` больше не удаляется, `setup_nginx()` заново создаёт нужные каталоги, а repeated install после живой ноды теперь проходит без ручного восстановления `/etc/nginx`.
- [2026-04-06 04:34:59] Legacy entrypoint `x-ui-pro.sh` фактически превращён в wrapper над `x-ui-pro-updated.sh` для всех практических сценариев; старое тело скрипта остаётся в файле как технический долг, но больше не должно исполняться по поддерживаемому пути.
- [2026-04-06 04:34:59] Дополнительно закрыты баги архитектурной привязки `xray-linux-amd64`, строгой проверки только первого `A`-адреса домена и жёсткого открытия только `22/tcp` в `ufw`.
- [2026-04-06 04:44:23] Актуальная GitHub-ревизия `aca03e2` уже повторно проверена на staging: новые `x-ui-pro.sh` и `x-ui-pro-updated.sh` загружены на сервер, `stage=verify` завершился полным `PASS`, repeated install через `sslip.io` снова завершился успешно и повторная verify-проверка осталась зелёной.
- [2026-04-06 04:44:23] Отдельный документ `docs/REGRESSION_CHECKLIST.md` уже создан; обязательный маршрут staging-проверок теперь формализован и не должен держаться только в памяти или scattered notes.
- [2026-04-06 04:48:57] Внешняя client-side проверка уже выполнена с локальной Windows-машины: web-sub страница `https://185.207.64.40.sslip.io/SuPFrSWtjs/?name=first`, `clashmeta/first` и `sub2sing-box` endpoint отдают корректный ответ по `HTTPS`.
- [2026-04-06 04:48:57] В `clashmeta/first` подтверждены локальные URL форка для probe и `rule-providers`, но в HTML интерфейсе `sub2sing-box` обнаружена внешняя ссылка на `unpkg.com`; это становится следующим точечным кандидатом на устранение.
- [2026-04-06 04:52:02] Дополнительный аудит HTML `sub2sing-box` уточнил объём задачи: помимо `unpkg.com` UI использует ещё и `fonts.googleapis.com` для шрифтов и иконок, поэтому следующий этап уже является не точечным патчем, а отдельным подэтапом локализации клиентских ассетов `sub2sing-box`.
- [2026-04-06 05:13:22] Подэтап локализации `sub2sing-box` UI закрыт: локальные `mdui`/font/icon ассеты добавлены в `vendor/lib/sub2sing-box-ui`, `nginx` runtime HTML переписывается на локальные URL, а внешние raw JSON `sb-rule-sets` заменяются на локальные файлы форка.
- [2026-04-06 05:13:22] На staging VPS `stage=websub -verify yes` после этого патча завершился полным `PASS`, а внешний client-side smoke-test подтвердил `200 OK` на локальные `mdui.css`, `mdui.global.js`, `sub2sing-box-fonts.css`, `material-icons.css`, `material-icons-outlined.css` и локальные template JSON.
- [2026-04-06 05:13:22] Внешние runtime-утечки `sub2sing-box` UI на `unpkg.com`, `fonts.googleapis.com` и `github.com/legiz-ru/sb-rule-sets/raw/...` больше не воспроизводятся на живой staging-ноде.
- [2026-04-06 05:17:31] `x-ui-pro.sh` дочищен до финального совместимого wrapper-entrypoint: историческое installer-тело физически удалено из файла, поэтому поддерживаемый путь теперь структурно совпадает с документированной архитектурой.
- [2026-04-06 05:28:47] Следом за этим найден и закрыт ещё один runtime-хвост: `nginx` warning `duplicate MIME type "text/html"` в `sub2sing-box` proxy-блоке устранён, потому что нормализатор теперь вычищает legacy `sub_filter_types text/html;` из уже установленного `includes.conf`, не вставляя её обратно.
- [2026-04-06 05:28:47] После фикса на staging заново подтверждены `stage=websub`, repeated install через `x-ui-pro.sh` и финальный `stage=verify`; install-лог больше не содержит `duplicate MIME type`, а `nginx -t` остаётся чистым.
- [2026-04-06 05:38:01] Следующий слой user-facing cleanup тоже уже пройден: `sub-3x-ui.html` больше не содержит `gozargah_marzban`, `Gozargah/Marzban#donation` и placeholder `example.com/path/to/template.json`, а `sub-3x-ui-classical.html` переведён с `BLUEBL0B/Secret-Sing-Box` на актуальный `legiz-ru/Secret-Sing-Box`.
- [2026-04-06 05:38:01] `stage=verify` теперь умеет ловить forbidden upstream URL и неожиданные внешние ссылки вне allowlist как в web-sub странице, так и в `sub2sing-box` UI; staging уже подтвердил зелёный проход этого нового контроля.
- [2026-04-06 05:46:14] Для внешних бинарников уже включён явный pinning: installer принимает `XUI_REPO_SLUG`, `XUI_VERSION`, `SUB2SINGBOX_REPO_SLUG`, `SUB2SINGBOX_VERSION`, `SUB2SINGBOX_ARCH`, а `.env.example` содержит проверенные значения `v2.8.11` и `v0.0.9`.
- [2026-04-06 05:46:14] Дополнительно устранён ещё один лишний внешний хвост: `x-ui.sh` больше не скачивается отдельно с `main`, а копируется из tarball-релиза той же pinned-версии `3x-ui`.
- [2026-04-06 05:46:14] Staging уже подтвердил repeated install с явным пиннингом `XUI_VERSION=v2.8.11`, `SUB2SINGBOX_VERSION=v0.0.9`, `SUB2SINGBOX_ARCH=amd64`; install-лог показывает pinned-версии, а финальный `stage=verify` остаётся зелёным.
- [2026-04-06 08:17:42] Для следующего этапа собран единый подробный паспорт форка `docs/FORK_PASSPORT.md`; именно его теперь нужно использовать как главный источник по истории upstream, нашим изменениям, багфиксам, regression-проверкам и текущему состоянию проекта.
- [2026-04-06 20:52:47] Найден и исправлен новый реальный runtime-баг `VLESS Reality`: installer парсил вывод `xray x25519` по неверным шаблонам и мог записывать пустой `PublicKey`; генерация ключей исправлена, а `stage=verify` теперь проверяет валидность `REALITY`-пары ключей прямо в `x-ui.db`.
- [2026-04-06 21:03:30] По live-выводу staging-сервера уточнено, что у текущего `xray x25519` публичный клиентский ключ приходит как `Password:`; parser форка обновлён под этот формат, поэтому следующий install-run уже должен корректно создавать `REALITY`-inbound.
- [2026-04-07 00:56:41] На новом полностью отформатированном VPS повторно подтверждён весь критический участок `REALITY`: clean install завершился успешно, `stage=verify` дал полный `PASS`, а `stream_settings` для inbound `8443` содержат валидный `settings.publicKey`.
- [2026-04-07 01:12:08] Для основного `amd64`-сценария закрыт следующий крупный кусок автономности: `3x-ui v2.8.11` и `sub2sing-box v0.0.9` уже зеркалированы в `vendor/releases/`, installer использует их локально из клона и проверяет по `SHA256SUMS`.
- [2026-04-07 01:12:08] На чистом staging VPS это уже подтверждено фактическим install-логом: обе строки `Using local mirrored release archive: ...` появились во время установки, а финальный `stage=verify` после такого install снова завершился полным `PASS`.
- [2026-04-07 01:16:59] Проверенный install-контур уже перенесён в `main`, поэтому официальный install-source репозитория и реальная staging-проверка теперь совпадают.
- [2026-04-08 07:11:12] Следующий profile-aware срез в `codex/platform-v2` уже выполнен: `load_existing_runtime_context()` теперь дополнительно поднимает `reality_domain`, а `verify_existing_installation()` различает runnable baseline `classic` и staged-профили, для которых public-HTTPS проверки пока ещё не обязательны.
- [2026-04-08 07:11:12] Текущая точка продолжения сместилась на следующий явный runtime-этап: открыть runnable transport-path для `stealth-xray`, не трогая пока `AWG` и не ослабляя проверенный install-контур `classic`.
- [2026-04-08 21:18:12] Этот runtime-этап уже закрыт: `stealth-xray` больше не staged-only, а открыт как runnable-path в `codex/platform-v2`; для него сознательно сохранён мягкий verify до отдельного Linux smoke-test.
- [2026-04-08 21:34:41] Этот Linux smoke-test уже тоже закрыт: на staging `185.207.64.40` подтверждены clean install, `-stage verify`, отсутствие `stream.conf`, связка `xray:443 -> nginx:7443`, а также реальные `200 OK` для panel/web-sub/sub2sing-box/fallback root через публичный `443`.
- [2026-04-08 21:58:42] Этот этап тоже уже закрыт: `stealth verify` стал strict и profile-aware, staging подтверждает полный `PASS` не только для install-path, но и для повторной диагностики установленной stealth-ноды.
- [2026-04-08 22:29:01] После этого точка продолжения уточнена: переход к `AWG` пока откладывается, потому что текущий этап считается не завершённым до формального закрытия всего `Xray/DPI` слоя.
- [2026-04-08 22:29:01] Текущая каноническая точка продолжения — документ `XRAY_DPI_PLAN.md`: сначала довести `stealth` до завершённой anti-DPI матрицы и полного operational parity, а уже потом открывать `AWG`.
- [2026-04-08 22:29:01] Отдельный блок ускорений тоже не потерян: он считается следующим обзорным этапом после закрытия `Xray/DPI` слоя и должен быть напомнен при переходе к следующей волне работ.

## Что ещё НЕ завершено

- [2026-04-05 05:58:19] Не проведён следующий этап рефакторинга shell-логики по модулям.
- [2026-04-06 05:13:22] В `sub2sing-box` HTML всё ещё остаются осознанные пользовательские внешние ссылки на GitHub/App Store/Google Play; это уже не runtime-ассеты, а отдельный вопрос продуктового решения, нужно ли их зеркалировать.

## Наиболее логичный следующий шаг

- [2026-04-07 01:16:59] Аудит `x-ui-pro-old.sh` завершён: legacy-файл признан опасным для случайного запуска и переведён в блокирующий archive-mode с немедленным `exit 1`.
- [2026-04-07 01:44:24] Блокировка legacy-файла уже подтверждена на staging VPS: `x-ui-pro-old.sh` возвращает `RC=1`, печатает предупреждение и не останавливает активный `x-ui`.
- [2026-04-07 02:25:32] Следующий срез shell-рефакторинга уже начат: в `x-ui-pro-updated.sh` нормализован активный вывод `print_execution_plan()`, `verify_existing_installation()` и `main()`, а staging повторно подтвердил новые сообщения для `verify`, `websub` и общего `dry-run`.
- [2026-04-07 02:37:53] Следующий срез модульной чистки уже закрыт: дублирующие нижние определения `print_execution_plan()`, `verify_existing_installation()` и `main()` удалены, а staging повторно подтвердил `verify` и все актуальные dry-run маршруты на единственном каноническом наборе функций.
- [2026-04-07 03:53:02] Следующий этап проекта переведён в design-mode: в `docs/ARCHITECTURE_NEXT.md` зафиксированы baseline current-state, будущий профиль `stealth/zero-signature`, сохранение двух доменов и модель `AmneziaWG` как отдельного модуля без немедленного изменения installer-кода.
- [2026-04-07 03:53:02] Ближайший практический этап после этого — собрать точное техническое ТЗ новой архитектуры, а не писать код вслепую; baseline install-контур и текущий regression-маршрут должны оставаться неизменными до отдельной feature-ветки.
- [2026-04-07 04:00:36] `ARCHITECTURE_NEXT.md` дополнен внешне подтверждёнными ограничениями: `fallback` не считается автоматически каноническим механизмом для `REALITY`, `AWG` не может молча сделать проект Docker-first, а двухдоменная модель пользователя закреплена как инвариант нового этапа.
- [2026-04-07 04:00:36] Ближайший следующий шаг после этого — собрать уже финальную матрицу режимов `classic/stealth/AWG optional` и критерии готовности для кода, после чего только открывать реализацию installer-логики.
- [2026-04-07 04:23:02] Финальная матрица нового этапа уже собрана в `PLATFORM_BLUEPRINT.md`: baseline зафиксирован как `classic`, `stealth` оформлен как отдельный profile-gated режим, а `AmneziaWG` — как отдельный `optional module`.
- [2026-04-07 04:23:02] В `PLATFORM_BLUEPRINT.md` также зафиксированы product-модель платформы, anti-DPI стратегия по `Xray`, local-first supply chain для `AWG`, целевая структура репозитория `core/modules/providers/web/docker/vendor/tests/docs` и поэтапный план реализации.
- [2026-04-07 04:23:02] Текущий ближайший безопасный кодовый этап теперь определён точно: сначала ввести абстракцию профилей и провайдеров без изменения поведения `classic`, и только после этого открывать отдельные implementation-ветки для `stealth-xray` и `AWG`.
- [2026-04-08 04:30:47] Но перед этим сначала закрывается организационный срез: сохранить единый канонический порядок по веткам и документам, а уже затем открывать новую большую implementation-ветку от стабильной базы.
- [2026-04-08 04:55:17] Ближайший следующий практический шаг после текущего этапа — уже кодовый: начать работу в `codex/platform-v2` по `PLATFORM_BLUEPRINT.md`, сохраняя `classic` baseline неизменным до появления абстракции профилей и провайдеров.
- [2026-04-08 05:09:48] Следующий практический шаг после текущего среза — осторожно перевести на новый selection-layer внутренние decision points installer'а так, чтобы `classic` продолжал работать 1-в-1, но код уже перестал быть жёстко прибитым к одному профилю и одному panel provider.
- [2026-04-08 05:34:52] Следующий практический шаг после текущего среза — перевести на selection-layer runtime-defaults и конфигурационные boundary-значения `classic`, сохранив тот же install-result и только потом открывать отдельные новые runtime-профили.
- [2026-04-08 05:58:05] Этот шаг уже закрыт: `classic` теперь берёт длины токенов, политику динамических портов, публичные и внутренние baseline-порты, а также локальный порт `sub2sing-box` из selection-layer без изменения install-result.
- [2026-04-08 06:09:36] Этот шаг уже закрыт: дефолты `3x-ui` для блока `settings` теперь берутся из provider metadata, а SQL больше не хранит их как единственный источник правды.
- [2026-04-08 06:21:05] Этот шаг тоже уже закрыт: bootstrap-дефолты панели и минимальная поддерживаемая версия `3x-ui` теперь идут через provider metadata/helper-layer, а не через жёсткий install-flow.
- [2026-04-08 06:32:49] Этот шаг уже начат в коде: `stealth-xray` добавлен как staged-profile в selection-layer, имеет metadata, runtime-state и dry-run visibility, но ещё не имеет открытого install-path.
- [2026-04-08 06:44:51] Этот шаг уже частично закрыт: ingress-layer `nginx` разделён на `classic` и `stealth` через отдельные функции и dispatcher-слой `platform_setup_ingress/platform_enable_ingress`.
- [2026-04-08 07:04:32] Следующий шаг уже тоже частично закрыт: transport/runtime seam `stealth-xray` выделен через orchestrator `update_xui_db()` и отдельный helper `write_transport_inbounds_stealth_xray()`, а transport metadata теперь явно различают `REALITY`-контракт двух профилей.
- [2026-04-08 07:04:32] Текущая точка продолжения: следующим срезом нужно сделать `verify_existing_installation()` и `load_existing_runtime_context()` profile-aware, после чего можно будет переводить `stealth` из staged-состояния в реальный runnable-профиль; `AWG` пока не трогаем.
- [2026-04-07 01:16:59] Отдельно нужно решить судьбу оставшихся product-facing внешних ссылок, которые сознательно ещё не зеркалированы.
- [2026-04-11 17:50:00] Текущая ближайшая точка продолжения изменилась: сначала нужно повторно прогнать зарубежный staging уже на новом `verify`-контуре, потому что старые `PASS` для `stealth-xhttp` оказались недостаточными без реальной transport-пробы.
- [2026-04-11 17:50:00] Следующий практический шаг теперь такой: обновить сервер до новой ревизии `codex/platform-v2`, запустить `-stage verify -debug yes -keep_artifacts yes`, посмотреть результат `xhttp-selftest`, и только после этого снова выходить в ручной клиентский тест.
