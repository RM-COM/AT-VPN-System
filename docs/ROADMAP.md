# ROADMAP

## Текущий спринт

- [2026-04-05 07:13:07] Детализированный главный план проекта вынесен в `MASTER_PLAN.md`; этот файл остаётся краткой дорожной картой, а `MASTER_PLAN.md` используется как основная рабочая карта этапов.
- [2026-04-08 04:30:47] Перед новым большим обновлением текущий спринт временно переведён в организационный режим: сначала нормализуются ветки, точки входа в `docs/` и канонические документы, а уже потом открывается новая implementation-ветка.

- [2026-04-05 05:53:47] Довести форк до максимально автономной работы из локального репозитория без зависимости от чужих repo-owned ресурсов.
- [2026-04-05 05:53:47] Сформировать постоянную русскоязычную документацию проекта в `docs/`.
- [2026-04-05 05:53:47] Зафиксировать текущую архитектуру, ограничения и следующую очередь работ.

## Следующие шаги

- [2026-04-05 07:03:16] Проверить на staging пользовательские сценарии уже как клиент: открытие реальной web-sub страницы, выдачу `clash.yaml`, работу `sub2sing-box`, сохранность debug-артефактов после install-потока.
- [2026-04-06 03:10:17] Контур `stage=reset -> install -> verify -> websub -> backup -> uninstall -> restore -> verify` уже подтверждён на staging VPS и может считаться базовым regression-маршрутом форка.
- [2026-04-06 04:34:59] Дополнительно подтверждён clean install success-path и repeated install success-path через `x-ui-pro.sh` на доменах `sslip.io`; installer переиспользует существующие сертификаты и корректно возвращает `SSH`-сеанс после завершения.
- [2026-04-06 04:44:23] Актуальная GitHub-ревизия повторно подтверждена на staging уже после публикации: `stage=verify` и repeated install снова прошли успешно на серверной копии.
- [2026-04-06 04:48:57] Пользовательские URL и клиентские артефакты уже проверены извне: web-sub и `clash` отдаются корректно, а `sub2sing-box` endpoint доступен по `HTTPS`.
- [2026-04-06 05:13:22] Локализация `sub2sing-box` UI закрыта: `mdui`, локальные font/icon CSS и runtime template URL теперь обслуживаются локально, а внешние client-side runtime-утечки на staging не воспроизводятся.
- [2026-04-06 05:17:31] Technical debt в `x-ui-pro.sh` закрыт: wrapper физически очищен, поэтому следующий шаг смещается на управляемый список осознанных пользовательских внешних ссылок и дальнейший shell-рефакторинг.
- [2026-04-06 05:28:47] Дополнительно закрыт runtime-regress после локализации `sub2sing-box` UI: warning `duplicate MIME type` устранён, а `stage=websub`, repeated install и финальный `stage=verify` снова подтверждены на staging.
- [2026-04-06 05:38:01] Осознанные пользовательские внешние ссылки переведены в контролируемый режим: старые upstream URL убраны, project links вынесены в env/placeholders, а verify получил allowlist/forbidden-проверку.
- [2026-04-06 05:46:14] Внешние бинарники переведены на управляемый pinning, а install больше не смешивает tarball-релиз `3x-ui` с отдельным raw `x-ui.sh` из `main`.
- [2026-04-07 01:12:08] Для основного `amd64`-контура решение уже принято и подтверждено runtime-тестом: проверенные release-архивы `3x-ui v2.8.11` и `sub2sing-box v0.0.9` зеркалированы в `vendor/releases/` и используются локально при install из клона.
- [2026-04-07 01:16:59] `main` уже синхронизирован с проверенным install-контуром и может считаться официальным источником установки.
- [2026-04-07 01:16:59] Аудит `x-ui-pro-old.sh` завершён: файл оставлен как архивный артефакт и заблокирован от исполнения.
- [2026-04-07 02:25:32] Следующий срез shell-рефакторинга уже закрыт: активный console-output `x-ui-pro-updated.sh` для `verify`, `websub` и общего `dry-run` нормализован, а staging подтвердил отсутствие битых строк в этих маршрутах.
- [2026-04-07 02:37:53] Следующий шаг этой же волны shell-рефакторинга тоже закрыт: временные дублирующие определения функций удалены, а staging подтвердил тот же зелёный `verify/websub/install-dry-run` уже на одном каноническом наборе функций.
- [2026-04-07 03:53:02] Перед следующей волной кода открывается отдельный архитектурный этап: нужно формализовать будущий профиль `stealth/zero-signature`, сохранить baseline install-контур, удержать двухдоменную модель и спроектировать опциональную интеграцию `AmneziaWG`.
- [2026-04-07 03:53:02] Теперь фокус временно смещается с shell-рефакторинга на design-stage: сначала архитектурное ТЗ и границы изменений, затем feature-ветка и только потом код новой install-логики.
- [2026-04-07 04:00:36] Design-stage усилен внешней и repo-aware перепроверкой: теперь отдельно зафиксированы ограничения `REALITY/fallback`, статус `Docker` как optional-layer для `AWG`, а также правило использовать параллельных агентов в больших анализах.
- [2026-04-07 04:00:36] Следующий шаг остаётся прежним по сути, но стал точнее: финализировать матрицу режимов `classic/stealth/AWG optional`, а не переходить к коду на уровне общих гипотез.
- [2026-04-07 04:23:02] Этот шаг закрыт: канонический `PLATFORM_BLUEPRINT.md` собрал продуктовую модель платформы, anti-DPI стратегию, local-first контур `AWG`, целевую структуру репозитория и этапы реализации.
- [2026-04-08 04:30:47] Следующий ближайший шаг уже уточнён: сначала завершить организационный порядок через `BRANCH_MATRIX.md`, `DOCS_MATRIX.md`, `PROJECT_STATE.md` и `OPERATIONS.md`, а затем открыть новую платформенную implementation-ветку от стабильной базы.
- [2026-04-08 04:55:17] Этот шаг уже закрыт: рабочей веткой следующего большого этапа теперь является `codex/platform-v2`, причём она стартует от `main` и уже содержит безопасный подготовительный слой из `legacy-audit` и `stealth-awg-architecture`.
- [2026-04-08 05:09:48] Первый кодовый срез в `codex/platform-v2` уже закрыт: модульный каркас `core / modules / providers` и selection-layer внедрены без изменения поведения `classic`.
- [2026-04-08 05:34:52] Следующий шаг внутри `platform-v2` уже тоже закрыт: безопасные orchestration decision points installer'а переведены на selection-layer без открытия `stealth` и `AWG`.
- [2026-04-08 05:58:05] Этот шаг уже закрыт: runtime-defaults и конфигурационные boundary-значения `classic` переведены на тот же selection-layer, а baseline-install result сохранён.
- [2026-04-08 06:09:36] Этот шаг тоже уже закрыт: provider-level defaults `3x-ui` вынесены в metadata/selection-layer без изменения baseline-result.
- [2026-04-08 06:21:05] Этот последний безопасный подготовительный срез тоже закрыт: panel bootstrap/defaults верхнего уровня вынесены из install-flow.
- [2026-04-08 06:32:49] Этот шаг уже закрыт в staged-виде: `stealth-xray` существует как профиль selection-layer и уже проходит локальные metadata/check-комбинации.
- [2026-04-08 06:44:51] Следующий безопасный шаг тоже уже закрыт: `nginx` ingress разделён на `classic` и подготовительный `stealth` seam, при этом `classic` сохранён без изменения результата, а transport-path `stealth` всё ещё защитно закрыт.
- [2026-04-08 07:04:32] Следующий transport/runtime шаг тоже уже закрыт: `stealth-xray` получил собственный helper и metadata-контракт для `REALITY`, при этом baseline `classic` сохранён без изменения install-result.
- [2026-04-08 07:11:12] Этот profile-aware шаг уже закрыт: `verify_existing_installation()` и `load_existing_runtime_context()` различают strict baseline `classic` и staged `stealth`, не ломая при этом текущий regression-контур.
- [2026-04-08 21:18:12] Этот runnable-шаг уже закрыт: `stealth-xray` переведён в `ready`, но отдельный Linux staging-regression для него ещё не пройден.
- [2026-04-08 21:34:41] Этот Linux smoke-test уже закрыт: `stealth-xray` прошёл clean install, отдельный `-stage verify` и ручные HTTPS-пробы через публичный `443` на staging VPS.
- [2026-04-08 21:58:42] Этот strict verify шаг уже закрыт: `stealth` теперь проверяется как полноценный profile-aware runtime-контур, а не как staged-исключение.
- [2026-04-08 22:29:01] Ближайший шаг уточнён: перед `AWG` сначала нужно закрыть текущий `Xray/DPI` этап как отдельный продуктовый блок, а не считать `stealth` автоматически завершённым только по факту рабочего install-path.
- [2026-04-08 22:29:01] Для этого введён отдельный фазовый документ `XRAY_DPI_PLAN.md`, где зафиксированы критерии готовности, ближайшие задачи и правило не смешивать текущую anti-DPI работу с внедрением `AWG`.
- [2026-04-08 22:29:01] Идеи по ускорению системы не теряются: они вынесены в следующий обзорный блок после закрытия `Xray/DPI` этапа и не должны забываться при планировании.
- [2026-04-08 23:00:27] Блок A из `XRAY_DPI_PLAN.md` закрыт: текущий `stealth` baseline уже имеет operational parity и подтверждён staging-регрессом.
- [2026-04-08 23:00:27] Следующий активный шаг roadmap теперь — блок B: открыть второй stealth transport, предпочтительно `REALITY + XHTTP`, как отдельную selection-комбинацию.
- [2026-04-08 23:00:27] `AWG` и будущий блок ускорений по-прежнему не поднимаются выше по приоритету: они следуют только после завершения transport hardening и anti-DPI матрицы для `Xray`.
- [2026-04-08 23:24:11] Блок B уже закрыт практическим staging-подтверждением: `stealth-xhttp` открыт как второй runnable stealth transport, установлен на `185.207.64.40` и проходит строгий `verify`.
- [2026-04-08 23:24:11] Дополнительно подтверждён autodetect нового transport-профиля без ручной передачи env: `load_existing_runtime_context()` сам восстанавливает `stealth-xhttp` по `x-ui.db`.
- [2026-04-08 23:24:11] Следующий активный шаг roadmap теперь — блок C: собрать anti-DPI матрицу приёмки, user-facing проверки и критерии выбора между `stealth-xray` и `stealth-xhttp`.
- [2026-04-05 05:53:47] Свести оставшиеся пользовательские внешние ссылки к управляемому списку: что зеркалим, что оставляем внешним сознательно.

## Бэклог

- [2026-04-05 06:17:58] Расширить debug-mode до поэтапных режимов `packages`, `ssl`, `nginx`, `xui` и добавить более подробный smoke-check локального `sub2sing-box`.
- [2026-04-05 06:17:58] Добавить отдельный regression-check для генерации `REALITY`-ключей и валидации заполнения `x-ui.db`.
- [2026-04-05 06:56:44] Решить, хотим ли мы сохранить `QEMU`-предупреждение как warning навсегда или сделать его настраиваемым через отдельный флаг/переменную окружения.
- [2026-04-05 07:04:56] Подготовить первую feature-ветку следующего этапа от `main`, чтобы не вести дальнейшие изменения напрямую в базовой ветке.
- [2026-04-05 05:53:47] Ввести явный режим пиннинга версий внешних бинарников через переменные окружения.
- [2026-04-05 05:53:47] Добавить автоматическую проверку ссылочной целостности `vendor/` и `fake-site/templates/`.
- [2026-04-05 05:53:47] Разбить крупные shell-скрипты на более модульную структуру или библиотеку функций.
- [2026-04-05 05:53:47] Подготовить отдельную release-ветку и схему атомарных коммитов с `Conventional Commits`.
- [2026-04-08 04:30:47] После организационного этапа открыть следующую большую implementation-ветку платформенного уровня от стабильной базы и перевести в неё работу по `classic/stealth/AWG` как по отдельным модулям.
