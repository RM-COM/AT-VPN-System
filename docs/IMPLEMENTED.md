# IMPLEMENTED

## Что уже работает

- [2026-04-05 05:53:47] Установщик в `x-ui-pro.sh` и `x-ui-pro-updated.sh` умеет брать страницу подписки, `Clash`-шаблоны и `vendor/` из локального клона репозитория.
- [2026-04-05 05:53:47] `sub-3x-ui.html` и `sub-3x-ui-classical.html` используют локальные шаблоны `sing-box` и локальные frontend-библиотеки из `vendor/lib/`.
- [2026-04-05 05:53:47] `clash/*.yaml` используют локальные rule-provider URL внутри `${WEB_PATH}`.
- [2026-04-05 05:53:47] `vendor/sb-rule-sets/*.json` переведены на локальные URL для rule-set бинарников и JSON-файлов.
- [2026-04-05 05:53:47] В репозитории подготовлено локальное зеркало `vendor/rule-assets/` с 52 файлами, которые раньше тянулись извне через шаблоны `sing-box`.
- [2026-04-05 05:53:47] В репозитории подготовлен каталог `vendor/` с 88 файлами ассетов, библиотек, правил и зеркалированных источников.
- [2026-04-05 05:53:47] В `fake-site/templates/` находится локальный набор шаблонов; `randomfakehtml.sh` выбирает шаблон только локально.
- [2026-04-05 05:53:47] Синтаксис изменённых shell-скриптов проверен через `bash -n`.
- [2026-04-05 06:17:58] `x-ui-pro-updated.sh` поддерживает диагностические режимы `-stage verify` и `-stage websub` без полной переустановки сервера.
- [2026-04-05 06:17:58] `x-ui-pro-updated.sh` умеет собирать debug-артефакты в `/root/x-ui-pro-debug/<timestamp>/` и сохранять туда лог, конфиги `nginx`, дамп проверок и копию `x-ui.db`.
- [2026-04-05 06:17:58] `x-ui-pro.sh` умеет безопасно перекидывать `debug/stage`-запуск в `x-ui-pro-updated.sh` до начала своей деструктивной очистки.
- [2026-04-05 06:17:58] Исправлена генерация `REALITY` public key и исправлен порядок чтения старой `x-ui.db` перед очисткой предыдущей установки.
- [2026-04-05 06:56:44] Shell-скрипты форка подготовлены к нормальной работе на Linux после разработки с Windows: line endings нормализованы, а в репозиторий добавлен `.gitattributes` с принудительным `LF`.
- [2026-04-05 06:56:44] Installer больше не блокирует типичный виртуализированный VPS по признаку `QEMU Virtual CPU`; теперь это предупреждение, а не hard-fail.
- [2026-04-05 06:56:44] Реальный staging smoke-test на Ubuntu 24.04.4 с `-auto_domain yes` завершён успешно: сервисы `nginx`, `x-ui`, `sub2sing-box` поднялись, `x-ui.db` создана, web-sub отвечает по локальному HTTPS, `-stage verify` проходит полностью.
- [2026-04-05 07:04:56] Рабочий GitHub-репозиторий уже инициализирован: первый commit отправлен в `origin/main`, и дальнейшая работа может вестись обычным циклом `commit -> push -> staging-test`.
- [2026-04-05 07:06:52] В проекте уже присутствуют базовые сценарии сопровождения: полное удаление через `-uninstall` в installer-скриптах и резервное копирование/восстановление через `backup.sh`.
- [2026-04-05 07:23:54] `README.md` полностью приведён к текущей реальности форка `AT-VPN-System`: описывает наш `origin`, наш install/debug workflow, обязательный локальный клон, поддерживаемые сценарии `install`, `verify`, `stage=websub`, `uninstall`, `backup`, `restore`.
- [2026-04-05 07:23:54] В проектных правилах зафиксировано обязательное завершение каждого логического этапа через `commit + push` в `origin`, чтобы GitHub всегда оставался актуальной контрольной точкой перед staging-тестами.
- [2026-04-05 07:35:53] `x-ui-pro-updated.sh` теперь поддерживает `-stage reset`: staging-ноду можно привести к предсказуемо чистому состоянию перед новым install-прогоном без ручной последовательности команд.
- [2026-04-05 07:35:53] Reset-сценарий требует явного подтверждения `-confirm_reset yes`, сохраняет pre-reset артефакты и выполняет post-check после очистки.
- [2026-04-05 07:35:53] Полное удаление install-контура теперь чище: `uninstall_xui()` убирает остатки `sub2sing-box`, `web-sub`, `nginx`, `certbot`, каталоги сертификатов и бинарники.
- [2026-04-05 08:02:47] Полный staging-цикл `reset -> install -> verify -> websub` теперь подтверждён в реальном runtime на VPS `185.207.64.40`.
- [2026-04-05 08:02:47] Исправленный clean install после reset успешно поднимает `nginx`, `x-ui`, `sub2sing-box`, создаёт `x-ui.db`, разворачивает `/var/www/subpage` и проходит `nginx -t`.
- [2026-04-06 03:10:17] `backup.sh` теперь покрывает полноценный backup/restore operational-контура форка: сохраняет `nginx`/SSL, базу и runtime `x-ui`, `sub2sing-box`, `root` `crontab` и web-root каталоги, а при restore восстанавливает их и перезапускает сервисы.
- [2026-04-06 03:10:17] Legacy entrypoint `x-ui-pro.sh -uninstall yes` теперь использует актуальный uninstall-механизм `x-ui-pro-updated.sh`, а не исторический частичный сценарий.
- [2026-04-06 03:10:17] Полный operational regression `backup -> uninstall -> restore -> verify` уже подтверждён реальным runtime-прогоном на staging VPS `185.207.64.40`.
- [2026-04-06 04:15:15] `sub2sing-box` в поддерживаемом install-контуре теперь работает как `systemd`-сервис `sub2sing-box.service`, а не как фоновый процесс из `su ... & disown` и не как `@reboot` cron-задача.
- [2026-04-06 04:15:15] `x-ui-pro.sh -install yes` теперь перенаправляет установку в `x-ui-pro-updated.sh`, поэтому основной install-поток у форка фактически один и все новые исправления попадают в него сразу.
- [2026-04-06 04:15:15] Runtime-проверка service-контура подтверждена на staging: `stage=verify` видит активный `sub2sing-box service`, а `backup -> uninstall -> restore -> verify` проходит успешно уже в service-модели.
- [2026-04-06 04:34:59] Clean install через совместимый entrypoint `x-ui-pro.sh` теперь подтверждён реальным runtime-прогоном с доменами `sslip.io`: install-поток доходит до конца, корректно возвращает `SSH`-сеанс после финального экрана и не выглядит как «зависший».
- [2026-04-06 04:34:59] Repeated install поверх уже работающей ноды теперь устойчив: installer повторно использует существующие сертификаты `Let's Encrypt`, не ломает базовую структуру `nginx` при cleanup и после повторного запуска снова проходит `stage=verify`.
- [2026-04-06 04:34:59] Установщик теперь учитывает архитектуру helper-бинарника `xray` и не привязан к `amd64`; генерация `REALITY`-ключей и `uuid` берёт корректный бинарник `xray-linux-*` по фактической архитектуре.
- [2026-04-06 04:34:59] Firewall-контур безопаснее для staging и нестандартного `SSH`: `ufw` открывает не только `22/tcp`, но и реально используемые `SSH`-порты, обнаруженные в текущей системе.
- [2026-04-06 04:34:59] Legacy entrypoint `x-ui-pro.sh` фактически стал совместимым wrapper-слоем над `x-ui-pro-updated.sh`; основной install/debug/uninstall-контур уже был сведен к единому коду.
- [2026-04-06 05:17:31] `x-ui-pro.sh` окончательно дочищен до физического wrapper-entrypoint: историческое installer-тело удалено из файла, поэтому поддерживаемый путь теперь не только логически, но и структурно один.
- [2026-04-06 04:44:23] Актуальная ревизия форка повторно подтверждена на staging уже после публикации в GitHub: server-side `stage=verify` остаётся зелёным, а repeated install success-path через `x-ui-pro.sh` снова проходит на живой ноде без ручного вмешательства.
- [2026-04-06 04:44:23] Для дальнейшей работы подготовлен единый формальный regression-checklist, который теперь задаёт минимальный обязательный набор staging-проверок перед следующей волной изменений.
- [2026-04-06 04:48:57] Клиентский внешний контур уже частично подтверждён не только через локальный `stage=verify`, но и извне: web-sub HTML, `clashmeta/first` и endpoint `sub2sing-box` отвечают по реальному `HTTPS` URL staging-ноды.
- [2026-04-06 04:48:57] Выдаваемый `clash`-конфиг уже использует локальные URL нашего форка для probe/rule-provider файлов; локализация `clash`-контента подтверждена внешней проверкой, а не только статическим аудитом шаблонов.
- [2026-04-06 05:13:22] `sub2sing-box` UI теперь обслуживается через локальный bundle форка: `mdui`, локальные icon/font CSS, локальные `woff2` и локальные `sb-rule-sets` template URL подставляются прямо в runtime HTML через `nginx`.
- [2026-04-06 05:13:22] `stage=websub` теперь умеет дотягивать этот UI-fix и на уже установленную ноду: он обновляет web bundle, нормализует proxy-блок `sub2sing-box`, валидирует `nginx` и перезагружает его без полной переустановки сервера.
- [2026-04-06 05:13:22] Client-side smoke-test после фикса подтверждает уже не просто доступность `sub2sing-box` endpoint, а отсутствие runtime-утечек на `unpkg.com`, `fonts.googleapis.com` и внешние raw JSON `sb-rule-sets`.
- [2026-04-06 05:28:47] Нормализация `sub2sing-box` proxy-блока в `stage=websub` теперь полностью идемпотентна: старые строки `sub_filter_types text/html;` удаляются из уже установленного `includes.conf`, а `nginx -t` после повторного применения не даёт warning про duplicate MIME.
- [2026-04-06 05:28:47] Повторный install через новый чистый wrapper `x-ui-pro.sh` снова подтверждён на staging уже после этого фикса: runtime-лог остаётся чистым от `duplicate MIME`, а финальный `stage=verify` проходит полностью.
- [2026-04-06 05:38:01] Web-sub страница теперь использует проектные ссылки `PROJECT_SUPPORT_URL` и `PROJECT_DONATE_URL`, которые можно переопределить через окружение; по умолчанию они указывают на репозиторий `AT-VPN-System`.
- [2026-04-06 05:38:01] `stage=verify` теперь проверяет не только runtime-ассеты, но и пользовательские внешние ссылки: устаревшие upstream URL и неожиданные внешние домены вне allowlist считаются regression-ошибкой.
- [2026-04-06 05:38:01] На staging подтверждено, что web-sub больше не содержит `gozargah_marzban`, `Gozargah/Marzban#donation`, `example.com/path/to/template.json` и `BLUEBL0B/Secret-Sing-Box`.
- [2026-04-06 05:46:14] Внешние бинарники теперь управляются явно: install-путь поддерживает `XUI_REPO_SLUG`, `XUI_VERSION`, `SUB2SINGBOX_REPO_SLUG`, `SUB2SINGBOX_VERSION`, `SUB2SINGBOX_ARCH`, а `.env.example` уже содержит проверенные значения по умолчанию.
- [2026-04-06 05:46:14] `3x-ui` CLI теперь берётся из того же tarball-архива, что и остальная payload-часть релиза, поэтому install не смешивает pinned-версию tarball с отдельным raw-файлом из `main`.
- [2026-04-06 05:46:14] На staging подтверждён install/repeated install с явным пиннингом `v2.8.11` для `3x-ui` и `v0.0.9` для `sub2sing-box`; verify после такого прохода остаётся полностью зелёным.
- [2026-04-06 20:52:47] Генерация `REALITY`-ключей в installer теперь привязана к фактическому формату вывода `xray x25519`; пустой `PublicKey` или `PrivateKey` больше не может тихо попасть в базу панели.
- [2026-04-06 20:52:47] `stage=verify` теперь дополнительно проверяет `REALITY`-inbound внутри `x-ui.db` и падает, если у него отсутствует валидная пара `private/public key`.
- [2026-04-06 21:03:30] Для текущего `xray` в составе `3x-ui v2.8.11` installer уже учитывает, что клиентский `REALITY` `publicKey` берётся из строки `Password:` вывода `xray x25519`.
- [2026-04-07 00:56:41] Чистая установка на полностью отформатированном staging-сервере повторно подтверждена: `REALITY publicKey` после install реально записывается в `x-ui.db`, а не только проходит локальную shell-валидацию на этапе генерации.
- [2026-04-07 01:12:08] Для основного проверенного `amd64`-сценария release-архивы `3x-ui` и `sub2sing-box` уже зеркалированы внутрь форка в `vendor/releases/` и подтверждены реальной clean install-проверкой на staging.
- [2026-04-07 01:12:08] Installer в локальном clone-сценарии использует зеркалированные архивы из `vendor/releases/` с проверкой по `vendor/releases/SHA256SUMS`; скачивание с upstream остаётся только как fallback для неподготовленных версий или архитектур.

## Как использовать сейчас

- [2026-04-05 05:53:47] Поддерживаемый режим запуска форка: `git clone` -> `cd x-ui-pro` -> `sudo bash ./x-ui-pro.sh -install yes -panel 1 -ONLY_CF_IP_ALLOW no`.
- [2026-04-05 05:53:47] Для полной локальной раздачи ассетов нужен именно запуск из локального клона; raw one-liner не является основным сценарием этого форка.
- [2026-04-05 05:53:47] После установки web-sub открывается по адресу `https://<домен>/<web_path>/?name=first`.
- [2026-04-05 06:17:58] Для диагностики действующей установки можно использовать `sudo bash ./x-ui-pro-updated.sh -stage verify -debug yes -keep_artifacts yes`.
- [2026-04-05 06:17:58] Для быстрой локальной переустановки только web-sub контура можно использовать `sudo bash ./x-ui-pro-updated.sh -stage websub -verify yes -debug yes`.
- [2026-04-05 06:17:58] Для безопасного предпросмотра полной установки без изменения системы можно использовать `sudo bash ./x-ui-pro-updated.sh -dry_run yes -install yes -panel 1 -subdomain <домен> -reality_domain <домен>`.
- [2026-04-05 06:56:44] Для установки с автоматическими тестовыми доменами можно использовать `sudo bash ./x-ui-pro-updated.sh -install yes -panel 1 -auto_domain yes -debug yes -keep_artifacts yes -verify yes`.
- [2026-04-06 03:10:17] Для резервного копирования и восстановления состояния панели/`nginx` используется `sudo bash ./backup.sh`; этот путь уже подтверждён отдельным runtime-регрессом на staging.
- [2026-04-06 03:10:17] Для полного удаления через поддерживаемый legacy entrypoint можно использовать `sudo bash ./x-ui-pro.sh -uninstall yes`; теперь этот путь делегирует выполнение в актуальный uninstall-контур форка.
- [2026-04-06 04:15:15] Для поддерживаемой установки через совместимый entrypoint можно использовать `sudo bash ./x-ui-pro.sh -install yes ...`; этот путь теперь тоже делегирует выполнение в `x-ui-pro-updated.sh`.
- [2026-04-06 04:34:59] Для повторной установки поверх уже работающей ноды можно использовать ту же команду `sudo bash ./x-ui-pro.sh -install yes ...`; если сертификаты для тех же доменов уже существуют и валидны, installer переиспользует их и не расходует лишний `certbot`-лимит.
- [2026-04-06 04:44:23] Для обязательного regression-прогона теперь нужно ориентироваться на `docs/REGRESSION_CHECKLIST.md`; это основной маршрут перед новыми багфиксами, рефакторингом и перед публикацией следующего этапа.
- [2026-04-06 05:13:22] После локализации `sub2sing-box` UI достаточно `sudo bash ./x-ui-pro-updated.sh -stage websub -verify yes -debug yes -keep_artifacts yes`, чтобы применить web/UI-патч к уже установленной ноде без полного reinstall.
- [2026-04-07 01:12:08] Для проверки локальных release-архивов staging-install теперь должен выполняться именно из актуального локального клона репозитория на сервере; только в этом режиме install-лог обязан показать `Using local mirrored release archive: ...`.
- [2026-04-05 07:01:41] Локальный git-репозиторий уже переведён на схему `origin = AT-VPN-System`, `upstream = mozaroc/x-ui-pro`, базовая ветка — `main`.
- [2026-04-05 07:23:54] Нормативный цикл публикации теперь жёстко фиксирован: после каждого завершённого этапа сначала выполняются `commit` и `push` в `origin`, и только затем следующий этап или очередной staging-прогон.
- [2026-04-05 07:35:53] Для следующего runtime-этапа готов новый сценарий проверки: `stage reset -> install -> verify -> websub -> uninstall -> backup -> restore`.
- [2026-04-06 03:10:17] Operational-контур теперь подтверждён целиком: `stage reset -> install -> verify -> websub -> backup -> uninstall -> restore -> verify`.

## История действий и доступов

- [2026-04-05 05:53:47] Локально был исследован репозиторий `mozaroc/x-ui-pro`.
- [2026-04-05 05:53:47] Для сравнения содержимого были клонированы внешние репозитории-источники в рабочую папку сравнения: `legiz-ru/x-ui-pro` и `GFW4Fun/randomfakehtml`.
- [2026-04-05 05:53:47] Для восстановления upstream-ассетов были загружены изображения `marz-sub-page`, frontend-библиотеки и rule-set файлы в локальный `vendor/`.
- [2026-04-05 05:53:47] Подключений к production-базам данных, внешним приватным API и пользовательским серверам в рамках этого этапа не выполнялось.
- [2026-04-05 05:53:47] На этом этапе изменения внесены только в локальную копию репозитория; деплой на серверы не выполнялся.
- [2026-04-05 05:58:19] В документации зафиксированы результаты первичного аудита проекта: назначение репозитория, риски, сильные/слабые стороны и список известных проблем.
- [2026-04-05 06:17:58] На этом этапе деплой на реальные серверы всё ещё не выполнялся; новый debug-mode проверен только синтаксически и требует smoke-test на отдельной Linux-ноде.
- [2026-04-05 06:56:44] Выполнено реальное подключение по `SSH` к staging VPS `185.207.64.40`, туда загружена текущая рабочая копия форка и проведён полный install/smoke-test вне production-контура.
- [2026-04-05 07:01:41] Подключён рабочий GitHub-репозиторий `https://github.com/RM-COM/AT-VPN-System.git`; доступ на `push` проверен через `git push --dry-run`.
- [2026-04-05 07:04:56] Первый публичный commit форка опубликован в `https://github.com/RM-COM/AT-VPN-System.git` на ветке `main`.
- [2026-04-06 03:10:17] На staging-ноду загружена актуальная ревизия ветки `codex/operational-regression`, после чего последовательно выполнены `stage=verify`, `backup.sh`, `x-ui-pro.sh -uninstall yes`, `backup.sh restore` и финальный `stage=verify`.
- [2026-04-06 03:10:17] Backup текущего regression-этапа создан в каталоге `/backup/2026-04-06/00-08-08`; restore из него завершился успешно и вернул рабочие сервисы без ручных правок.
- [2026-04-06 04:15:15] Для service-regression был создан новый backup `/backup/2026-04-06/01-13-17`; в архив `sub2sing-box-01-13-17.tar.gz` уже входит `usr/bin/sub2sing-box` и `etc/systemd/system/sub2sing-box.service`.
- [2026-04-06 04:15:15] Повторный clean install retest через `x-ui-pro.sh -install yes` был остановлен внешним rate limit Let's Encrypt, а не внутренней ошибкой install-кода; staging восстановлен в рабочее состояние через `backup.sh restore`.
- [2026-04-06 04:34:59] Для обхода внешнего rate limit был выполнен отдельный clean install и repeated install regression с доменами `185.207.64.40.sslip.io` и `185-207-64-40.sslip.io`; оба прохода завершились успешно, staging остался в рабочем состоянии.
- [2026-04-06 04:44:23] На этом же staging после публикации текущего commit повторно загружены обновлённые installer-скрипты и заново подтверждены `stage=verify` и repeated install; это фиксирует, что GitHub-ревизия и рабочее состояние стенда не расходятся.
- [2026-04-06 04:48:57] С локальной Windows-машины дополнительно проверены внешние URL staging-ноды; это закрывает первый client-side smoke-test после формализации regression-checklist.
- [2026-04-06 05:13:22] Повторный client-side smoke-test после локализации `sub2sing-box` UI подтвердил локальные `200 OK` на `mdui.css`, `mdui.global.js`, `sub2sing-box-fonts.css`, `material-icons.css`, `material-icons-outlined.css` и `vendor/sb-rule-sets/ru-bundle.json`.
- [2026-04-07 00:56:41] После полного форматирования staging VPS `185.207.64.40` снова выполнен чистый install из GitHub-ветки `codex/operational-regression`, затем `stage=verify`, а также отдельное чтение `REALITY`-inbound из `x-ui.db`; все проверки подтвердили, что installer создаёт валидный `REALITY`-контур на новой ноде без ручных правок.
- [2026-04-07 01:12:08] На этом же staging VPS после следующего clean install подтверждено фактическое использование локальных архивов `vendor/releases/3x-ui/...` и `vendor/releases/sub2sing-box/...`; install завершился успешно и сразу закрыл финальный `stage=verify`.
- [2026-04-07 01:16:59] Проверенный install-контур больше не ограничен рабочей веткой: `main` синхронизирован с последней проверенной ревизией и теперь является официальным источником установки из GitHub-репозитория.
