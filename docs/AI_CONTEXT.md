# AI_CONTEXT

## Архитектурные решения

- [2026-04-05 05:53:47] Базовая модель проекта: один shell-установщик разворачивает `3x-ui`, `nginx`, `certbot`, `sqlite3`, `ufw` и настраивает маршрутизацию через `443`.
- [2026-04-05 05:53:47] В текущем форке поддерживаются два обслуживаемых домена: основной домен для панели, web-sub, WS/XHTTP/Trojan gRPC и отдельный домен для `REALITY`.
- [2026-04-05 05:53:47] `nginx` используется как входная точка и SNI-мультиплексор: трафик делится между web-частью и Xray-контурами.
- [2026-04-05 05:53:47] Web-sub страница и `Clash`-шаблоны раздаются из `/var/www/subpage`, а не из внешних GitHub raw URL.
- [2026-04-05 05:53:47] Для статических web-ресурсов принят единый локальный контейнер `vendor/` внутри репозитория.
- [2026-04-05 07:01:41] Git-топология форка стандартизована: рабочий репозиторий проекта — `origin`, исходный код поставщика — `upstream`.

## Структура локального bundle-набора

- [2026-04-05 05:53:47] `vendor/lib/` — локальные frontend-библиотеки для web-sub страницы.
- [2026-04-05 05:53:47] `vendor/upstream/marz-sub-page/` — upstream-изображения и favicon-ассеты страницы.
- [2026-04-05 05:53:47] `vendor/sb-rule-sets/` — JSON-шаблоны `sing-box`.
- [2026-04-05 05:53:47] `vendor/rules/` — локальные правила для `Clash`-шаблонов.
- [2026-04-05 05:53:47] `vendor/rule-assets/` — зеркала внешних `rule-set` binary/source файлов, которые ранее тянулись из GitHub.
- [2026-04-05 05:53:47] `vendor/probes/` — локальные probe-файлы для health-check и `url-test`.
- [2026-04-07 01:12:08] `vendor/releases/` — локальные зеркала release-архивов `3x-ui` и `sub2sing-box` для проверенных версий и архитектур; контроль целостности ведётся через `vendor/releases/SHA256SUMS`.

## База данных и сервисный контур

- [2026-04-05 05:53:47] Основная база панели — SQLite: `/etc/x-ui/x-ui.db`.
- [2026-04-05 05:53:47] Ключевые таблицы, в которые пишет установщик: `settings`, `client_traffics`, `inbounds`.
- [2026-04-05 05:53:47] Установщик напрямую модифицирует `x-ui.db`, создавая первичные inbound-конфигурации и параметры подписок.
- [2026-04-05 06:17:58] Для debug-mode добавлено обратное чтение runtime-контекста из уже установленной системы: `webPort`, `webBasePath`, `subPath`, `subJsonPath`, `subURI`, `subJsonURI` читаются из `x-ui.db`, а `sub2singbox_path` — из `/etc/nginx/snippets/includes.conf`.

## Диагностический контур

- [2026-04-05 06:17:58] Источником правды для диагностики выбран `x-ui-pro-updated.sh`; legacy-скрипт `x-ui-pro.sh` не реализует debug-mode самостоятельно, а передаёт выполнение в обновлённый сценарий.
- [2026-04-05 06:17:58] Текущая версия debug-mode поддерживает `-stage verify` для проверки существующей установки и `-stage websub` для переустановки только web-sub контура.
- [2026-04-05 07:35:53] Debug-контур расширен до `-stage reset`: staging reset использует тот же артефактный корень `/root/x-ui-pro-debug/<timestamp>/`, но добавляет pre-reset снимки и post-check чистого состояния.
- [2026-04-05 07:49:59] Для `nginx` выбран более устойчивый модульный подход: при динамическом `stream` модуле загрузка переводится в `/etc/nginx/modules-enabled/50-mod-stream.conf`, а `stream_geoip2` подключается отдельным файлом `/etc/nginx/modules-enabled/70-mod-stream-geoip2.conf`, чтобы не ломать порядок `load_module` после clean install.
- [2026-04-05 06:17:58] При `-debug yes` включается сохранение трассировки и debug-логов в `/root/x-ui-pro-debug/<timestamp>/run.log`.
- [2026-04-05 06:17:58] При `-keep_artifacts yes` сохраняются артефакты диагностики: `nginx.conf`, `includes.conf`, `stream.conf`, `index.html`, `clash.yaml`, копия `x-ui.db`, результаты `nginx -t`, `nginx -T` и `systemctl status`.
- [2026-04-05 06:17:58] При `-dry_run yes` для полного сценария выполняется только планирование без изменения системы; это не полноценная эмуляция каждой команды, а безопасный предпросмотр сценария.
- [2026-04-05 06:56:44] `-verify yes` теперь трактуется как пост-проверка install-потока, а не как скрытый алиас `stage=verify`; самостоятельный режим диагностики запускается только через явный `-stage verify`.
- [2026-04-06 04:15:15] `sub2sing-box` переведён на штатное управление через `systemd`: canonical runtime-юнит теперь `/etc/systemd/system/sub2sing-box.service`, а проверка install-состояния должна смотреть прежде всего на `systemctl is-active sub2sing-box`.
- [2026-04-06 04:34:59] Legacy entrypoint `x-ui-pro.sh` в поддерживаемом режиме больше не является самостоятельным installer-контуром: он служит совместимым wrapper-слоем и передаёт выполнение в `x-ui-pro-updated.sh`, что снижает риск расхождения между двумя ветками install-логики.
- [2026-04-06 05:17:31] Wrapper-модель закреплена структурно: `x-ui-pro.sh` больше не хранит параллельную install-реализацию даже как unreachable code, поэтому риск регрессии из-за случайного возврата в старое тело скрипта устранён.

## Сетевые и инфраструктурные допущения

- [2026-04-05 05:53:47] Целевые ОС: Debian 12+ / Ubuntu 24+.
- [2026-04-05 05:53:47] Проект рассчитан на запуск от `root`.
- [2026-04-05 05:53:47] Сертификаты выпускаются через `certbot`, поэтому публичный DNS должен указывать на сервер корректно.
- [2026-04-05 05:53:47] Полная автономность пока не достигнута: внешними остаются `3x-ui`, `sub2sing-box`, `Let's Encrypt`, `ipwho.is`, `icanhazip` и ряд пользовательских ссылок.
- [2026-04-05 06:56:44] Реальный staging-прогон успешно прошёл на Ubuntu 24.04.4 VPS с `QEMU Virtual CPU`, поэтому текущая политика форка: предупреждать о виртуальном CPU, но не блокировать выполнение.
- [2026-04-05 07:06:52] Канонический pipeline окружений теперь формализован так: локальная разработка и сборка на Windows -> публикация в GitHub `origin` -> загрузка на Linux staging -> запуск install/verify/reset/regression-сценариев.
- [2026-04-06 04:34:59] Валидация домена для installer-контуров смягчена под реальные DNS-сценарии: домен считается корректно направленным на сервер, если любой его `A`-адрес совпадает с текущим `IP4`, а не только самый первый ответ резолвера.
- [2026-04-06 04:34:59] Для repeated install принят принцип reuse-before-renew: при наличии уже выпущенного и ещё валидного сертификата installer не должен инициировать новый `certbot certonly`, чтобы не ловить лишние лимиты `Let's Encrypt` на staging и при повторных деплоях.
- [2026-04-06 04:34:59] Cleanup перед repeated install должен удалять только runtime-остатки форка и локальные generated-конфиги, но не базовую файловую структуру пакета `nginx`; этот инвариант уже подтверждён staging-регрессом после исправления cleanup-логики.
- [2026-04-06 04:34:59] Сетевой safeguard форка теперь учитывает нестандартный `SSH`: перед включением `ufw` installer пытается открыть и текущий порт из `SSH_CONNECTION`, и все `port`-значения из `sshd -T`, а fallback на `22` используется только если определить порты не удалось.

## Контур сопровождения и миграции

- [2026-04-05 07:06:52] Режим полного удаления через installer и режимы `backup/restore` рассматриваются как обязательные сценарии сопровождения сервера, а не как второстепенные утилиты.
- [2026-04-05 07:06:52] Для безопасных повторных тестов нужен явный reset-поток staging-ноды, который возвращает сервер к чистому состоянию перед новым install-прогоном.
- [2026-04-05 07:35:53] Реализованный reset-поток deliberately не трогает `UFW` и произвольные пользовательские файлы вне install-контура форка, чтобы не рисковать SSH-доступом и сторонними данными на staging.
- [2026-04-05 07:49:59] Runtime-тест после `stage=reset` уже выявил один реальный баг clean reinstall и сразу дал материал для фикса; это подтверждает, что staging reset нужен не только как удобство, но и как инструмент обнаружения скрытых install-регрессий.
- [2026-04-05 08:02:47] После фикса модульной схемы `nginx` clean reinstall на staging снова становится воспроизводимым: `nginx -t` проходит, а последующие `stage=verify` и `stage=websub` отрабатывают без дополнительных ручных правок.
- [2026-04-06 03:10:17] Operational-модель сопровождения закреплена так: перед risky-изменениями выполняется `backup.sh`, legacy `-uninstall` обязан вести в актуальный uninstall-контур, а `restore` должен возвращать не только файлы, но и рабочие сервисы с валидным локальным HTTPS web-sub.
- [2026-04-06 03:10:17] `backup.sh` теперь считается частью поддерживаемой архитектуры форка, а не вспомогательным legacy-скриптом: он покрывает конфигурацию `nginx`, сертификаты, SQLite-базу панели, runtime `x-ui`, бинарник `sub2sing-box`, `root crontab` и web-root директории.
- [2026-04-06 04:15:15] Дополнительное внешнее ограничение staging: clean install retest с авто-доменами `*.cdn-one.org` может временно блокироваться exact-set rate limit у Let's Encrypt; это не дефект локального installer-кода, но его нужно учитывать при планировании повторных regression-прогонов.
- [2026-04-06 04:34:59] Для обхода внешнего exact-set rate limit в regression-практике уже подтверждён запасной путь: альтернативные публичные тестовые домены `sslip.io` позволяют заново прогонять clean install и repeated install, не откладывая проверку installer-кода.
- [2026-04-06 04:44:23] Формализованный regression-контур теперь вынесен в отдельный документ `REGRESSION_CHECKLIST.md`; он становится каноническим staging-маршрутом до тех пор, пока набор обязательных сценариев не изменится архитектурно.
- [2026-04-06 04:48:57] Клиентская внешняя проверка показала разделение остаточных зависимостей: web-sub и `clash` уже обслуживаются локально нашим сервером, но встроенный UI `sub2sing-box` всё ещё ссылается на `unpkg.com` как на внешний frontend CDN.
- [2026-04-06 04:52:02] Точный состав frontend-зависимостей `sub2sing-box` UI уже подтверждён внешней проверкой: `https://unpkg.com/mdui@2/mdui.css`, `https://unpkg.com/mdui@2/mdui.global.js`, `https://fonts.googleapis.com/css?family=Roboto|Noto+Sans+SC&display=swap`, `https://fonts.googleapis.com/icon?family=Material+Icons`, `https://fonts.googleapis.com/icon?family=Material+Icons+Outlined`.
- [2026-04-06 05:13:22] Для `sub2sing-box` выбран путь без модификации самого бинарника: HTML ответа переписывается на лету в `nginx` через `sub_filter`, а локальные ассеты и template JSON обслуживаются из `/var/www/subpage/vendor/`.
- [2026-04-06 05:13:22] В локальном font-bundle `sub2sing-box` UI используется прагматичная схема: `Roboto` и минимальные `Noto Sans SC` latin/cyrillic subset-файлы лежат локально, а CJK-глифы при необходимости добираются через системный fallback, без внешних запросов к Google Fonts.
- [2026-04-06 05:28:47] Для `sub_filter` принят инвариант: `text/html` не задаётся через `sub_filter_types`, потому что это базовый тип модуля и его явное указание даёт warning `duplicate MIME type`; нормализатор обязан вычищать такую legacy-строку при миграции существующей ноды.
- [2026-04-06 05:38:01] Для пользовательских внешних ссылок принят двухуровневый контроль: known-bad URL считаются forbidden, а оставшиеся внешние домены допускаются только через явную allowlist-политику в `verify`.
- [2026-04-06 05:38:01] Project-facing ссылки web-sub вынесены в переменные `PROJECT_REPO_URL`, `PROJECT_SUPPORT_URL`, `PROJECT_DONATE_URL`; это убирает жёсткую привязку страницы к старому upstream и делает форк самостоятельным на уровне user-facing navigation.
- [2026-04-06 05:46:14] Для внешних бинарников выбран этапный подход: сначала явный pinning проверенных версий через env и единый tarball-source-of-truth, а уже потом возможное зеркалирование release-артефактов внутрь форка.
- [2026-04-06 05:46:14] `3x-ui` CLI больше не рассматривается как отдельная внешняя зависимость от `main`-ветки: версионная согласованность достигается копированием `x-ui.sh` из уже скачанного tarball-релиза.
- [2026-04-07 01:12:08] Стратегия внешних бинарников переведена в гибридный режим: для основного `amd64`-контура форк уже содержит локальные release-архивы и использует их в install-потоке, а для остальных комбинаций версия/архитектура сохраняется fallback на upstream.
- [2026-04-07 01:12:08] Canonical install-путь локальных архивов теперь такой: `local_repo_asset_path(<rel_path>)` -> `copy_or_fetch_release_archive()` -> `verify_release_archive_checksum()` -> распаковка в runtime; при отсутствии локального архива installer явно предупреждает и только потом обращается к upstream URL.
- [2026-04-06 20:52:47] Для генерации `REALITY`-ключей canonical source of truth — фактический текстовый вывод `xray x25519`; парсинг должен поддерживать реальный формат `Private key:` / `Public key:` и не должен опираться на выдуманные поля вида `PrivateKey:` / `PublicKey:`.
- [2026-04-06 20:52:47] Проверка install-состояния теперь включает не только сервисы, web/UI и локальные ассеты, но и целостность `REALITY`-inbound в `x-ui.db`, потому что сломанный `pbk` может делать ноду внешне «зелёной», но фактически непригодной для `VLESS Reality`.
- [2026-04-06 21:03:30] Для `xray-linux-amd64` из актуальной поставки `3x-ui v2.8.11` клиентский `REALITY` `publicKey` соответствует строке `Password:` в выводе `xray x25519`; это подтверждено прямым запуском бинарника на staging-сервере.
- [2026-04-07 00:56:41] Новый clean install на полностью отформатированной Ubuntu-ноде подтвердил, что текущий install-контур форка воспроизводим с нуля: parser `REALITY`, `nginx`, `x-ui`, `sub2sing-box`, SSL и локальный web/UI-контур поднимаются корректно без сохранённых артефактов прошлой установки.
- [2026-04-07 01:12:08] Следующий clean install на пустой staging-ноде дополнительно подтвердил, что для основного `amd64`-сценария install-контур уже воспроизводим без upstream-download этапа для `3x-ui` и `sub2sing-box`: лог явно фиксирует использование локальных `vendor/releases/*` архивов и успешный последующий `verify`.

## Ограничения текущего этапа

- [2026-04-06 04:34:59] End-to-end контур уже подтверждён не только на clean install, но и на repeated install поверх существующей ноды; ограничение смещается с базовой install-проверки на дальнейший рефакторинг, checklist и снижение внешних бинарных зависимостей.
- [2026-04-07 01:16:59] `x-ui-pro-old.sh` не включён в поддерживаемый контур, содержит исторические внешние зависимости и теперь дополнительно заблокирован для исполнения guard-блоком в начале файла.
- [2026-04-05 05:58:19] Результаты технического и безопасностного аудита вынесены в отдельный документ `REPOSITORY_AUDIT.md`, чтобы они не потерялись между этапами разработки.
- [2026-04-05 06:17:58] Debug-mode пока покрывает только сценарии `verify`, `websub` и безопасный `dry-run`; детальный поэтапный debug для всех install-стадий ещё не реализован.
- [2026-04-06 03:10:17] Full operational regression `backup -> uninstall -> restore -> verify` уже закрыт; следующий этап смещается с восстановления install-контура на формализацию regression-checklist, клиентские сценарии и дальнейший рефакторинг.
- [2026-04-06 04:44:23] Формализация regression-checklist закрыта на уровне документации; следующий шаг теперь смещается с описания маршрута на клиентские проверки и дальнейшую очистку technical debt.
- [2026-04-06 04:48:57] Первый client-side smoke-test уже выполнен; следующий шаг смещается с базовой проверки URL на устранение найденных остаточных внешних ссылок и косметического technical debt.
- [2026-04-06 05:13:22] Локализация runtime-зависимостей `sub2sing-box` UI закрыта; следующий шаг смещается с клиентских CDN/JSON-утечек на оставшийся technical debt и стратегию по внешним бинарникам.
- [2026-04-06 05:28:47] После устранения warning по `duplicate MIME type` текущий web/UI-контур считается стабилизированным; следующий практический фокус смещается с идемпотентности `nginx` на продуктовые внешние ссылки и дальнейший shell-рефакторинг.
- [2026-04-07 02:25:32] Для безопасной нормализации console-output в `x-ui-pro-updated.sh` принят временный переходный приём: новые определения `print_execution_plan()`, `verify_existing_installation()` и `main()` добавлены ближе к концу файла и runtime-переопределяют старые тела. Это снижает риск при точечном исправлении большого shell-файла, но остаётся техническим долгом до следующего модульного рефакторинга.
- [2026-04-07 02:25:32] Новым каноническим поведением installer считается англоязычный стабильный output для `dry-run` и `verify`-маршрутов; staging уже подтверждает именно этот активный runtime-путь, а не старые закодированные строки из исторических блоков файла.
