# CHANGELOG
- [2026-04-11 20:05:00] fix: закрыт реальный user-facing баг `XRAY JSON Subscription` для `v2rayNG/v2rayN` — upstream `3x-ui` генерировал `vless` outbound'ы в невалидном формате `settings.address/id/port`, из-за чего импортированные JSON-профили показывались как `null:null` и не могли стартовать как нормальные Xray-конфиги.
- [2026-04-11 20:05:00] feat: добавлен локальный bridge `subjson-rewrite` (`/usr/local/bin/subjson-rewrite.py` + `subjson-rewrite.service`), через который публичный `/${json_path}` теперь забирает raw JSON с локального `x-ui` sub-port и переписывает `vless`-конфиги в валидный `settings.vnext`-формат без ручного патча upstream бинарника.
- [2026-04-11 21:05:00] feat: добавлен отдельный `stealth-xhttp handoff-safe` preset для асимметричного сценария `LTE -> Wi‑Fi`: `XMUX` полностью отключён, TCP keepalive/timeout сокращены (`3/15/7000`), а `sockopt` доставляется через JSON-подписку для более быстрого разрыва старой сессии при смене сети.
- [2026-04-11 20:05:00] feat: `stage=verify` дополнительно проверяет `subjson-rewrite`, а публичный JSON endpoint валидируется не только по `200/non-HTML`, но и по реальной структуре `vnext`; staging-переустановка подтвердила, что `reality` и `xhttp` JSON-конфиги теперь приходят в корректном формате для `v2rayNG`.
- [2026-04-11 19:40:00] feat: `stealth-xhttp low-latency` теперь дополняет server-side inbound блоком `xhttpSettings.xmux`; staging-проверка подтвердила, что `xmux` сохраняется в `x-ui.db`, доходит до JSON subscription и проходит strict `verify`.
- [2026-04-11 19:40:00] feat: `stage=verify` расширен transport-aware проверкой `xmux` для `stealth-xhttp`: валидируются наличие/отсутствие блока, его поля (`maxConcurrency`, `maxConnections`, `cMaxReuseTimes`, `hMaxRequestTimes`, `hMaxReusableSecs`, `hKeepAlivePeriod`), а loopback self-test теперь использует тот же `xmux`, который отдаётся клиенту.
- [2026-04-11 19:40:00] docs: зафиксирован важный операторский контракт `stealth-xhttp low-latency`: handoff-тесты нужно проводить через `XRAY JSON Subscription`, потому что direct URI/QR формат upstream `3x-ui` не умеет передавать `xhttpSettings.xmux`.
- [2026-04-11 19:35:00] fix: installer `3x-ui` теперь явно включает `subJsonEnable=true`; раньше JSON subscription route физически не монтировался, из-за чего `https://<domain>/<subJsonPath>/<subid>` возвращал `404`.
- [2026-04-11 19:35:00] fix: `subJsonURI` приведён к реальному JSON endpoint (`https://<domain>/<subJsonPath>/`), а не к web-sub странице `/?name=`; `verify` дополнительно начал проверять локальный JSON subscription endpoint и валить установку, если он отвечает HTML или не отвечает вовсе.
- [2026-04-11 18:25:00] fix: найдена и устранена реальная server-side причина падения `stealth-xhttp` на staging: Xray inbound, сгенерированный `x-ui`, отбрасывал запросы с `invalid padding`, потому что server-side `xPaddingBytes` в preset'ах был уже, чем фактический client-side default padding.
- [2026-04-11 18:25:00] fix: для `stealth-xhttp` безопасный server-side диапазон `TRANSPORT_XHTTP_X_PADDING_BYTES` выровнен на `100-1000` в профильных defaults и preset'ах `mobile-safe`, `low-latency`, `aggressive-stealth`; узкие значения вроде `16-96` и `32-256` больше не применяются серверу.
- [2026-04-11 18:40:00] docs: `site realism` вынесен в явный backlog anti-probing hardening: оба публичных домена должны вести себя как правдоподобные живые HTTPS-сайты, а не как формальные технические заглушки.
- [2026-04-11 17:50:00] fix: `stealth-xhttp` переведён на новый базовый runtime-контракт `XHTTP mode=auto` вместо `packet-up`; это выравнивает профиль с официальным `nginx + TLS + XHTTP` направлением и убирает прежний неверный default для нашего stealth-контура.
- [2026-04-11 17:50:00] fix: генерация `nginx` для `XHTTP` обновлена под более жёсткий H2 reverse-proxy contract: `grpc_pass unix:/dev/shm/uds2023.sock;`, явные `client_*` лимиты и дополнительные `X-Real-IP/Forwarded` заголовки.
- [2026-04-11 17:50:00] feat: `stage=verify` теперь выполняет активный `stealth-xhttp` loopback self-test через локальный Xray client и публичный ingress; старые `PASS`, основанные только на socket/path, больше не считаются достаточным подтверждением работоспособности transport-слоя.
- [2026-04-11 16:05:00] feat: `stage=acceptance` получил structured comparative layer: поддерживаются метки `label/group/network/operator/time_window/client/notes`, сохраняются `acceptance/session-metadata.env`, `acceptance/matrix-row.json` и `acceptance/probe-results.jsonl`, а `summary.txt` теперь фиксирует эти поля рядом с итогом server-side probe.
- [2026-04-11 11:11:25] docs: в roadmap добавлен следующий отложенный backlog после `hardened-awg`: `Hysteria` зафиксирован как будущий transport/provider-модуль, а `MTProto Telegram` — как отдельный узкоспециализированный сервисный модуль; оба направления сознательно не смешиваются с текущим `Xray/DPI` и `AWG` этапом.
- [2026-04-11 10:28:01] docs: детализирован канонический roadmap в `PLATFORM_VISION_PLAN.md` и `PROTOCOL_HARDENING_PLAN.md`: активное anti-DPI ядро ограничено `stealth-xray` и `stealth-xhttp`, compatibility-layer отделён от основного stealth-направления, `ICMP` исключён как нецелевой transport для `VLESS + REALITY`, а дальнейший порядок работ разложен по фазам `C1 -> presets -> unified baseline -> admin/subscription hardening -> hardened-awg -> mobile whitelist fallback -> performance`.
- [2026-04-11 10:28:01] docs: зафиксирован новый продуктовый инвариант «стабильный клиентский контракт» в `PROJECT_CONNECTIVITY_REQUIREMENTS.md`, `PLATFORM_VISION_PLAN.md`, `RULES.md`, `MASTER_PLAN.md`, `PROJECT_STATE.md`, `RESUME_POINT.md`: штатное обновление подписки считается нормальным путём server-side эволюции, а полный re-import подписки и ручная перенастройка клиентов переведены в категорию исключений.
- [2026-04-11 00:05:00] Зафиксирован новый положительный полевой результат для текущего тестового контура `codex/platform-v2`: при ручном переключении между `Wi‑Fi` и мобильной сетью соединение продолжает работать без наблюдаемой длинной паузы на восстановление, а базовый web/video трафик проходит в обе стороны вне окна `mobile whitelist`.
- [2026-04-11 00:05:00] Для triage отдельно сохранено сравнение с прежним поведением старого `xhttp`-контура: там после смены сети наблюдалась задержка порядка `~50 секунд`, а в текущем тесте этот симптом не воспроизвёлся.
- [2026-04-11 14:40:00] feat: в `x-ui-pro-updated.sh` реализован управляемый preset-layer override для transport tuning; добавлены параметры `-reality_tuning_profile` и `-xhttp_tuning_profile` и preset'ы `default`, `mobile-safe`, `low-latency`, `aggressive-stealth`.
- [2026-04-11 14:40:00] feat: применение preset'ов встроено в основной startup-flow и в autodetect runtime-контекста, поэтому tuning теперь реально влияет на `main`, `verify` и `acceptance`, а не остаётся только metadata-заготовкой.
- [2026-04-11 14:40:00] feat: `verify_existing_installation()` теперь валидирует runtime tuning по `x-ui.db`, а `stage=acceptance` дополнительно сохраняет `acceptance/runtime-snapshot.env` и redacted `acceptance/xui-inbounds-summary.txt` для comparative triage.
- [2026-04-11 15:20:00] feat: install-контур теперь сохраняет sidecar provenance-файл `/etc/x-ui/runtime-provenance.env` с `platform/transport/tuning` контекстом; `backup.sh` копирует его в каталог backup, а `restore` сначала читает этот файл и корректно различает `stealth-xray` и `stealth-xhttp` до fallback на старую эвристику.

- [2026-04-10 23:30:48] Полностью пересобран визуальный слой `docs/branch-html/`: все HTML-страницы переведены на более подробный русскоязычный формат с назначением ветки, правилами установки, рисками, проверками, стоп-условиями и планом.
- [2026-04-10 23:30:48] Добавлен генератор `docs/branch-html/render-branch-html.ps1`, чтобы дальше обновлять branch HTML единым воспроизводимым способом, а не вручную править набор страниц.
- [2026-04-10 23:30:48] Обновлён общий стиль `docs/branch-html/assets/branch-report.css`: страницы стали ближе к читаемой проектной документации с крупными секциями, карточками, timeline-блоками, callout'ами и нормальной мобильной адаптацией.
- [2026-04-10 23:09:41] Добавлен `docs/PLATFORM_VISION_PLAN.md`: итоговая рабочая карта идеи форка, целевой архитектуры, технологий, threat model, staged-плана и критериев успеха.
- [2026-04-10 23:09:41] Добавлена HTML-страница `docs/branch-html/codex-platform-v2-strategy.html`, а канонические docs и branch HTML синхронизированы под новую стратегическую карту.
- [2026-04-10 23:05:05] Зафиксирован новый полевой сценарий `mobile whitelist / IP-level drop`: `Reality First` работает на Wi‑Fi, но на мобильной сети может давать `I/O read/write on closed pipe` при живом сервере; это не считается автоматическим доказательством runtime-сбоя `Xray`.
- [2026-04-10 23:05:05] `PROTOCOL_HARDENING_PLAN.md`, `XRAY_DPI_PLAN.md`, `PROJECT_CONNECTIVITY_REQUIREMENTS.md`, `RESUME_POINT.md`, `MASTER_PLAN.md` и `PROJECT_STATE.md` обновлены под будущий whitelist-resilient fallback-класс.
- [2026-04-10 23:00:59] Уточнена стратегия по `AWG`: дефолтный контейнерный запуск не считается production-safe соседом `Xray`, но hardened `AWG` остаётся будущим кандидатом после генерации уникальных параметров, закрытия UI, state/verify/backup и co-tenancy acceptance.
- [2026-04-10 23:00:59] `PROTOCOL_HARDENING_PLAN.md`, `PROJECT_CONNECTIVITY_REQUIREMENTS.md`, `MASTER_PLAN.md` и `RESUME_POINT.md` обновлены под отдельный будущий профиль `hardened-awg`.
- [2026-04-10 22:53:58] Зафиксирован новый triage-риск `single-IP co-tenancy`: совместная работа дефолтного `AmneziaWG` и `Xray` на одном IP может формировать общий fingerprint и провоцировать IP-level `RST`/drop деградацию.
- [2026-04-10 22:53:58] `PROTOCOL_HARDENING_PLAN.md`, `PROJECT_CONNECTIVITY_REQUIREMENTS.md`, `XRAY_DPI_PLAN.md`, `MASTER_PLAN.md`, `RESUME_POINT.md` и `PROJECT_STATE.md` обновлены под правило: `AWG` сначала разворачивается как отдельный/резервный контур до co-tenancy acceptance.
- [2026-04-10 22:48:10] Добавлен канонический технологический документ `docs/PROTOCOL_HARDENING_PLAN.md`: зафиксированы варианты усиления `REALITY/XHTTP/AWG`, модель защиты от `RST`/drop, auth hardening, controlled tuning и phased rollout.
- [2026-04-10 22:48:10] Канонический docs-слой синхронизирован под новый protocol hardening план: обновлены `INDEX.md`, `DOCS_MATRIX.md`, `PROJECT_STATE.md`, `RESUME_POINT.md`, `XRAY_DPI_PLAN.md`, `MASTER_PLAN.md`.
- [2026-04-10 20:19:11] В `RULES.md` добавлено обязательное project-safe правило публикации: в GitHub-документации и branch HTML/README допускается только технический контент о проекте без личного контекста.
- [2026-04-10 20:19:11] Добавлен visual-layer `docs/branch-html/`: общий `index.html`, единый стиль `assets/branch-report.css` и по 2 branch-specific страницы (`overview` + `progress`) для всех веток из `BRANCH_MATRIX.md`.
- [2026-04-10 20:07:20] Зафиксирован новый канонический документ `docs/PROJECT_CONNECTIVITY_REQUIREMENTS.md`: в нём собраны продуктовые выводы по устойчивой связи и рабочим сценариям, ограничениям по инфраструктуре, anti-RST подходу, staged-интеграции `AWG` и требованиям к операционной фиксации контекста.
- [2026-04-10 20:07:20] Канонический слой документации синхронизирован под новый документ: обновлены `INDEX.md`, `DOCS_MATRIX.md`, `MASTER_PLAN.md`, `RESUME_POINT.md`, `XRAY_DPI_PLAN.md`.
- [2026-04-10 19:41:17] На новом staging-сервере `217.199.253.102` повторно подтверждён runnable-контур `stealth/stealth-xhttp`: выполнены install, strict `-stage verify` и `-stage acceptance` (`5m/30s`) с полным `PASS`.
- [2026-04-10 19:41:17] Зафиксирован operational-нюанс деплоя: для выбора профиля через `sudo` нужно передавать env как `sudo env PLATFORM_PROFILE=... TRANSPORT_PROFILE=...`, иначе сервер поднимается с `classic` defaults.
- [2026-04-10 19:41:17] В `codex/platform-v2` включён controlled `mobile-safe` preset для `REALITY`-слоя stealth-контуров: `stealth-xray` и `stealth-xhttp` теперь используют `TRANSPORT_REALITY_TUNING_PROFILE=mobile-safe` и стабильный fingerprint `chrome` вместо `random`.
- [2026-04-09 00:30:57] В `codex/platform-v2` добавлен новый stage `acceptance` в `x-ui-pro-updated.sh`: для текущего stealth-профиля он выполняет строгий `verify`, затем гоняет повторяемые HTTPS-пробы `panel/web-sub/sub2sing-box/fallback`, сохраняет `acceptance/summary.txt` и пишет `manual-client-checklist.md` в debug artifacts.
- [2026-04-09 00:30:57] На staging `185.207.64.40` этот новый server-side helper уже подтверждён для `stealth-xhttp` и `stealth-xray`; оба режима прошли короткий soak `1 minute / 10 seconds` без ошибок по всем четырём probe-точкам.
- [2026-04-09 00:30:57] Документация блока C синхронизирована под новый контур: `README.md`, `OPERATIONS.md`, `ANTI_DPI_MATRIX.md`, `XRAY_DPI_PLAN.md`, `PROJECT_STATE.md`, `MASTER_PLAN.md`, `RESUME_POINT.md` и `ROADMAP.md` теперь отделяют server-side acceptance от ещё не закрытой живой клиентской приёмки.
- [2026-04-09 01:10:14] В блоке C зафиксирован первый положительный клиентский результат: `stealth-xray` выдержал около 10 минут нагрузки `4K`-видео и параллельного браузерного трафика через `v2rayN` без замеченных обрывов.
- [2026-04-09 01:10:14] Следующая практическая цель по матрице теперь формализована как симметричный клиентский тест `stealth-xhttp` на том же staging-сервере и по тому же сценарию, а не как новый transport-срез.

- [2026-04-08 22:29:01] В `codex/platform-v2` введён отдельный фазовый документ `docs/XRAY_DPI_PLAN.md`: текущий этап теперь формально разделён на завершение `Xray/DPI` слоя, последующий `AWG` и отдельный будущий блок ускорений.
- [2026-04-08 22:29:01] Канонические документы `INDEX.md`, `MASTER_PLAN.md`, `ROADMAP.md`, `RESUME_POINT.md` и `PROJECT_STATE.md` синхронизированы с новым фокусом: `AWG` больше не считается немедленным следующим шагом до закрытия `Xray/DPI` этапа.
- [2026-04-08 21:58:42] В `codex/platform-v2` усилен strict verify для `stealth`: `verify_existing_installation()` теперь дополнительно проверяет отсутствие `stream.conf`, связку `xray:443` и `nginx:7443`, привязку `REALITY` inbound к `443`, а также ответы panel и fallback root через публичный `443`.
- [2026-04-08 21:58:42] Для устойчивости этих проверок парсинг `REALITY` в runtime-контексте и verify переведён с хрупких `LIKE`/regex на `sqlite json_extract(...)`, поэтому `reality_domain` и port `443` теперь читаются из `x-ui.db` надёжно.
- [2026-04-08 21:58:42] На staging `185.207.64.40` новый strict verify для `stealth/stealth-xray` уже подтверждён: отдельный `-stage verify -debug yes -keep_artifacts yes` завершился полным `PASS` после живой stealth-установки.
- [2026-04-08 21:34:41] В `codex/platform-v2` исправлен новый Linux runtime-баг metadata-слоя: `.env`-файлы профилей и провайдеров могли уходить на staging с `CRLF`, из-за чего `platform_init()` возвращал значения вида `49152\r` и ломал arithmetic/runtime helper'ы во время `stealth` install-path.
- [2026-04-08 21:34:41] Для устранения бага расширен контроль line endings: в `.gitattributes` добавлены правила `*.env text eol=lf` и `.env.example text eol=lf`, а metadata-файлы и `.env.example` нормализованы в `LF`.
- [2026-04-08 21:34:41] После этого на staging `185.207.64.40` впервые подтверждён реальный Linux install-path для `stealth/stealth-xray` на доменах `185.207.64.40.sslip.io` и `185-207-64-40.sslip.io`: clean install завершился успешно, `-stage verify` вернул полный `PASS`, а ручные HTTPS-пробы через публичный `443` для panel/web-sub/sub2sing-box и root fallback вернули `200/OK`.
- [2026-04-08 21:18:12] В `codex/platform-v2` открыт первый runnable-path для `stealth-xray`: профиль `stealth` и transport `stealth-xray` переведены из `planned` в `ready` как в repo metadata, так и во встроенном fallback-слое installer'а.
- [2026-04-08 21:18:12] Для этого safe-slice сохранён мягкий verify-gate: `verify_existing_installation()` по-прежнему включает локальные HTTPS-пробы только для `classic`, поэтому `stealth` не получает ложный `FAIL` до отдельного Linux staging-regression.
- [2026-04-08 21:18:12] Локально подтверждены `bash -n` для `x-ui-pro-updated.sh`, `x-ui-pro.sh` и `core/platform-lib.sh`, `git diff --check` без содержательных ошибок и metadata self-check `classic=ready/ready`, `stealth=ready/ready`; отдельный Linux runtime smoke-test для `stealth` остаётся следующим шагом.
- [2026-04-08 07:11:12] В `codex/platform-v2` выполнен следующий безопасный profile-aware срез: `load_existing_runtime_context()` теперь дополнительно восстанавливает `reality_domain` из `x-ui.db`, не ломая baseline-порядок чтения `webPort/webBasePath/subPath/subJsonPath/subURI/subJsonURI`.
- [2026-04-08 07:11:12] `verify_existing_installation()` переведён на staged-aware ветвление: `classic` сохраняет строгие локальные HTTPS-пробы через публичный ingress, а staged-профили вроде `stealth/stealth-xray` больше не валятся на этих classic-only ожиданиях до открытия runnable transport-path.
- [2026-04-08 07:11:12] Локально подтверждено, что `bash -n` для `x-ui-pro-updated.sh`, `x-ui-pro.sh` и `core/platform-lib.sh` проходит, `git diff --check` чистый, а metadata self-check по-прежнему различает `classic=ready/ready` и `stealth=planned/planned`.
- [2026-04-08 07:04:32] В `codex/platform-v2` выполнен следующий transport/runtime срез для `stealth-xray`: `update_xui_db()` разрезан на orchestrator и transport helpers, а giant SQL для baseline больше не является единственной точкой правды.
- [2026-04-08 07:04:32] Для transport metadata добавлены параметры `REALITY`-контракта: `xver`, `acceptProxyProtocol` и режим выбора публичного `dest`, благодаря чему `classic` и `stealth` теперь различаются не только ingress-портами, но и явной transport-моделью.
- [2026-04-08 07:04:32] Реализован отдельный helper `write_transport_inbounds_stealth_xray()`: он готовит `REALITY` inbound для режима `Xray:443 -> nginx:7443`, но сам профиль всё ещё остаётся staged до отдельного profile-aware шага для `verify` и восстановления runtime-контекста.
- [2026-04-08 07:04:32] Локально подтверждено, что `bash -n` для `x-ui-pro-updated.sh`, `x-ui-pro.sh` и `core/platform-lib.sh` проходит, `git diff --check` чистый, а metadata self-check уже различает `classic: 8443/0/true/domain` и `stealth: 443/1/false/reality_domain`.

- [2026-04-08 06:44:51] В `codex/platform-v2` выполнен первый реальный ingress-only срез для `stealth`: `setup_nginx()` и `enable_nginx_sites()` разделены на `classic` и `stealth` реализации, а dispatcher-слой начал переключать ingress по `PLATFORM_PROFILE`.
- [2026-04-08 06:44:51] `classic` ingress оставлен без функционального изменения, а `stealth` ingress пока ограничен только подготовкой `nginx`-границы: без `stream.conf`, без включения transport runtime и без изменения `update_xui_db()`.
- [2026-04-08 06:44:51] Локально подтверждено, что `bash -n` для `x-ui-pro-updated.sh`, `x-ui-pro.sh` и `core/platform-lib.sh` проходит, `git diff --check` чистый, а metadata self-check по-прежнему отдаёт `classic=ready` и `stealth=planned`.

- [2026-04-05 05:53:47] Инициализирована директория `docs/` и создан базовый комплект проектной документации.
- [2026-04-05 05:53:47] Полностью переведён `README.md` на русский язык и обновлён под текущий режим работы форка через локальный клон репозитория.
- [2026-04-05 05:53:47] Обновлены `x-ui-pro.sh` и `x-ui-pro-updated.sh`: установщик теперь копирует локальный каталог `vendor/`, подставляет web-placeholder'ы и раздаёт web-sub страницу из локального bundle-набора.
- [2026-04-05 05:53:47] Обновлены `sub-3x-ui.html` и `sub-3x-ui-classical.html`: удалены зависимости от внешних repo-owned ресурсов, шаблоны `sing-box` теперь берутся из локального `vendor/sb-rule-sets`.
- [2026-04-05 05:53:47] Обновлены `clash/*.yaml`: rule-provider ссылки переведены на локальные URL внутри `${WEB_PATH}`.
- [2026-04-05 05:53:47] Создано локальное зеркало `vendor/rule-assets/` для 52 внешних rule-set файлов, на которые ссылались JSON-шаблоны `sing-box`.
- [2026-04-05 05:53:47] Переписаны `vendor/sb-rule-sets/*.json`: внешние GitHub URL заменены на локальные пути форка через `${WEB_PATH}`.
- [2026-04-05 05:53:47] Обновлён `randomfakehtml.sh`: выбор fake-site шаблона теперь работает только по локальной директории, два шаблона с авто-подгрузкой внешних ресурсов исключены из ротации.
- [2026-04-05 05:53:47] Удалены устаревшие `web-assets/*`, так как проект перешёл на новую структуру `vendor/upstream/marz-sub-page/*`.
- [2026-04-05 05:53:47] Выполнена синтаксическая проверка `bash -n` для `x-ui-pro.sh`, `x-ui-pro-updated.sh` и `randomfakehtml.sh`.
- [2026-04-05 05:58:19] Документация дополнена файлами `REPOSITORY_AUDIT.md` и `RESUME_POINT.md`, чтобы сохранить весь накопленный контекст по аудиту и текущей точке продолжения работ.
- [2026-04-05 06:17:58] В `x-ui-pro-updated.sh` внедрён первичный `debug-mode`: добавлены флаги `-debug`, `-dry_run`, `-verify`, `-stage`, `-skip_cleanup`, `-keep_artifacts`.
- [2026-04-05 06:17:58] В `x-ui-pro-updated.sh` добавлены диагностические сценарии `-stage verify` и `-stage websub`, позволяющие проверять и переустанавливать web-sub без полной переустановки стека.
- [2026-04-05 06:17:58] В `x-ui-pro-updated.sh` реализован сбор debug-артефактов в `/root/x-ui-pro-debug/<timestamp>/` с логом запуска, снимками конфигов и выводами проверок.
- [2026-04-05 06:17:58] В `x-ui-pro-updated.sh` добавлено восстановление runtime-контекста из установленной системы: значения читаются из `x-ui.db` и `/etc/nginx/snippets/includes.conf`.
- [2026-04-05 06:17:58] Исправлен баг порядка действий: `read_existing_xui_db()` теперь вызывается до очистки предыдущей установки, а не после удаления `/etc/x-ui`.
- [2026-04-05 06:17:58] Исправлен баг генерации `REALITY`-ключа: `public_key` теперь читается из строки `PublicKey`, а не из ошибочного `Password`.
- [2026-04-05 06:17:58] В `x-ui-pro.sh` добавлена ранняя передача диагностических флагов в `x-ui-pro-updated.sh`, чтобы legacy-скрипт не выполнял деструктивную очистку в debug/stage-режимах.
- [2026-04-05 06:17:58] Повторно выполнена синтаксическая проверка `bash -n` для `x-ui-pro-updated.sh` и `x-ui-pro.sh` после внедрения debug-mode.
- [2026-04-05 06:56:44] Добавлен `.gitattributes` с фиксацией `LF` для shell/config/web-файлов, чтобы форк не ломался на Linux после переноса с Windows.
- [2026-04-05 06:56:44] Нормализованы line endings для shell-скриптов форка; staging-проверка подтвердила, что `CRLF` реально ломал запуск `bash` на Ubuntu.
- [2026-04-05 06:56:44] Смягчена CPU preflight-проверка: детект `QEMU Virtual CPU` больше не валит installer, а даёт предупреждение и позволяет продолжить установку.
- [2026-04-05 06:56:44] Исправлена ошибка логики `-verify yes`: флаг больше не переключает сценарий в `stage=verify` до установки и теперь используется как пост-проверка после install-потока.
- [2026-04-05 06:56:44] Исправлен `EXIT trap`: сообщение об ошибке теперь выводит реальный код завершения, а не ложный `0`.
- [2026-04-05 06:56:44] Выполнен реальный smoke-test на staging VPS `185.207.64.40` под Ubuntu 24.04.4 с `-auto_domain yes`; после установки `-stage verify` вернул полный набор `PASS`.
- [2026-04-05 07:04:56] Локальный git-репозиторий переведён на рабочую схему `origin = RM-COM/AT-VPN-System`, `upstream = mozaroc/x-ui-pro`, ветка `main` отправлена в `origin`.
- [2026-04-05 07:04:56] Первый базовый commit форка создан и опубликован в `origin/main`: `5d17057808c6feffd6fa6fcbb9b3eb8711c69310`.
- [2026-04-05 07:23:54] `README.md` полностью переписан под форк `AT-VPN-System`: убраны устаревшие упоминания чужого форка, внешних заметок и нерелевантных ссылок; описание установки, debug-режимов и workflow приведено к нашему репозиторию.
- [2026-04-05 07:23:54] В `docs/RULES.md` добавлено правило обязательного `commit + push` в `origin` после каждого завершённого логического этапа с кодовыми, debug- или документационными изменениями.
- [2026-04-05 07:23:54] `README.md` приведён к корректной Windows-совместимой UTF-8 кодировке, чтобы русскоязычное содержимое читалось одинаково локально и в GitHub.
- [2026-04-05 07:25:33] `.gitattributes` расширен на `*.md`, чтобы `README.md` и документы из `docs/` стабильно сохранялись в `LF` и не дрейфовали по line endings между Windows и Linux.
- [2026-04-05 07:35:53] В `x-ui-pro-updated.sh` реализован новый staging-режим `-stage reset` с отдельным destructive-потоком очистки тестовой ноды.
- [2026-04-05 07:35:53] Для `stage=reset` добавлено обязательное подтверждение `-confirm_reset yes`; без него reset-сценарий не запускается.
- [2026-04-05 07:35:53] Полный `uninstall_xui()` усилен: теперь он удаляет не только `x-ui` и `nginx`, но и остатки `sub2sing-box`, `web-sub`, `certbot` и сервисных каталогов.
- [2026-04-05 07:35:53] В debug-контур добавлены pre-reset артефакты и post-check reset-сценария: состояние сервисов, файлов, бинарников и портов `80/443`.
- [2026-04-05 07:35:53] Документация дополнена отдельным регламентом `docs/STAGING_RESET.md`, а `README.md` и `docs/DEBUG_MODE.md` обновлены под новый reset-workflow.
- [2026-04-05 07:43:17] Shell-вывод нового `stage=reset` нормализован в ASCII English, чтобы staging-диагностика не страдала от кодировочных проблем в Linux-консоли.
- [2026-04-05 07:49:59] На runtime после `stage=reset` выявлен баг clean install: `nginx` падал на неверном порядке загрузки `ngx_stream_module` и `ngx_stream_geoip2_module`.
- [2026-04-05 07:49:59] В `x-ui-pro-updated.sh` исправлена модульная логика `nginx`: ложный детект `--with-stream=dynamic` устранён, legacy `load_module`-строки вычищаются, а динамические `stream`/`stream_geoip2` теперь подключаются через `modules-enabled` в устойчивом порядке.
- [2026-04-05 08:02:47] Реальный regression на staging подтверждён: `stage=reset` проходит успешно, clean install после reset снова поднимает `nginx`, `x-ui`, `sub2sing-box`, а `stage=verify` и `stage=websub` завершаются полным `PASS`.
- [2026-04-05 08:02:47] Зафиксировано поведение long-running install на staging: локальный SSH-клиент упёрся в таймаут вывода, но фактическое состояние сервера после команды было успешно подтверждено через отдельные `verify` и service-check команды.
- [2026-04-06 03:10:17] В `backup.sh` усилен operational-контур: полный backup теперь включает `nginx`, `Let's Encrypt`, `x-ui.db`, runtime `x-ui`, бинарник `sub2sing-box`, `root` `crontab` и web-root каталоги.
- [2026-04-06 03:10:17] В `backup.sh` доработан restore-поток: добавена установка prerequisites, восстановление `root` `crontab`, нормализация прав на бинарники, `restart` сервисов после распаковки архивов и локальная HTTPS-проверка web-sub после восстановления.
- [2026-04-06 03:10:17] В `backup.sh` исправлено дублирование backup-архивов web-root путей: пути теперь нормализуются без хвостовых `/`, поэтому `/var/www/html` больше не архивируется дважды.
- [2026-04-06 03:10:17] В `x-ui-pro.sh` добавлена ранняя передача флага `-uninstall` в `x-ui-pro-updated.sh`, чтобы legacy entrypoint использовал актуальный uninstall-контур форка.
- [2026-04-06 03:10:17] На staging VPS `185.207.64.40` подтверждён полный operational regression: `backup -> uninstall -> restore -> stage=verify` завершился полным `PASS` на ветке `codex/operational-regression`.
- [2026-04-06 04:15:15] В `x-ui-pro-updated.sh` `sub2sing-box` переведён с фонового запуска через `su ... & disown` на постоянный `systemd`-юнит `sub2sing-box.service`; это убирает ложное «подвисание» SSH-сессии после завершения install-потока.
- [2026-04-06 04:15:15] В `x-ui-pro-updated.sh` диагностика и reset-проверки усилены под новый service-контур: `stage=verify` теперь учитывает `sub2sing-box.service`, а reset/uninstall дополнительно удаляют unit-файл и symlink из `multi-user.target.wants`.
- [2026-04-06 04:15:15] В `x-ui-pro.sh` добавлена ранняя передача флага `-install` в `x-ui-pro-updated.sh`, чтобы поддерживаемая установка шла через единый актуальный installer-контур, а не через старую дублирующую реализацию.
- [2026-04-06 04:15:15] В `backup.sh` резервное копирование `sub2sing-box` расширено до бинарника и `systemd` unit-файла; restore теперь поднимает `sub2sing-box` через `systemctl`, а legacy `@reboot` cron-entry очищается при наличии service-файла.
- [2026-04-06 04:15:15] На staging VPS подтверждён service-regression: после ручного перевода существующей ноды на `sub2sing-box.service` сценарий `backup -> uninstall -> restore -> stage=verify` снова завершился полным `PASS`, уже с проверкой активного `sub2sing-box service`.
- [2026-04-06 04:15:15] Во время clean install retest через `x-ui-pro.sh -install yes` код дошёл до `certbot`, но staging упёрся во внешний лимит Let's Encrypt для exact set доменов `185.207.64.40.cdn-one.org` и `185-207-64-40.cdn-one.org`; повтор возможен после `2026-04-06 13:20:21 UTC` (`2026-04-06 16:20:21` по Москве) либо с другим набором доменов.
- [2026-04-06 04:34:59] В `x-ui-pro-updated.sh` усилена валидация DNS: `resolve_to_ip()` больше не требует совпадения только с первым `A`-ответом и теперь считает домен валидным, если любой `A`-адрес указывает на текущий `IP4`.
- [2026-04-06 04:34:59] В `x-ui-pro-updated.sh` добавлены `xray_binary_path()` и архитектурно корректная работа с helper-бинарником `xray-linux-*`; генерация `x25519` и `uuid` больше не жёстко прибита к `xray-linux-amd64`.
- [2026-04-06 04:34:59] В `x-ui-pro-updated.sh` смягчён cleanup repeated install: `clean_previous_install()` теперь удаляет только runtime-остатки форка и больше не сносит базовый layout `nginx`, чтобы повторная установка не ломалась из-за отсутствия `/etc/nginx/nginx.conf`.
- [2026-04-06 04:34:59] В `x-ui-pro-updated.sh` добавлено переиспользование уже выпущенных сертификатов: `obtain_ssl()` теперь пропускает новый вызов `certbot`, если действующий сертификат уже существует и валиден как минимум ещё 24 часа.
- [2026-04-06 04:34:59] В `x-ui-pro-updated.sh` усилен сетевой контур staging: `setup_ufw()` теперь открывает реальные `SSH`-порты, определённые из `SSH_CONNECTION` и `sshd -T`, а не только жёсткий `22/tcp`.
- [2026-04-06 04:34:59] В `x-ui-pro-updated.sh` `setup_nginx()` теперь гарантированно создаёт необходимые каталоги `modules-enabled`, `sites-available`, `sites-enabled`, `snippets`, `stream-enabled`, чтобы installer был устойчивым после частичной очистки системы.
- [2026-04-06 04:34:59] В `x-ui-pro.sh` добавлен безусловный безопасный wrapper: legacy entrypoint теперь передаёт выполнение в `x-ui-pro-updated.sh` не только для `install/debug/uninstall`, но и для остальных практических сценариев, не доходя до исторического деструктивного тела скрипта.
- [2026-04-06 04:34:59] На staging VPS подтверждён полный clean install success-path с альтернативными доменами `185.207.64.40.sslip.io` и `185-207-64-40.sslip.io`: установка завершилась успешно, `SSH`-сеанс вернулся без ложного «подвисания», а последующий `stage=verify` дал полный `PASS`.
- [2026-04-06 04:34:59] На том же staging VPS подтверждён repeated install success-path поверх уже работающей ноды: тот же install-командный путь прошёл повторно, installer переиспользовал существующие сертификаты для обоих `sslip.io` доменов, а финальный `stage=verify` снова завершился полным `PASS`.
- [2026-04-06 04:44:23] На staging VPS повторно загружена актуальная ревизия `aca03e2`; локальная runtime-проверка `stage=verify` на этой серверной копии снова завершилась полным `PASS`.
- [2026-04-06 04:44:23] На staging VPS повторно подтверждён repeated install success-path уже на текущем commit форка: `x-ui-pro.sh -install yes` завершился с кодом `0`, снова переиспользовал существующие сертификаты `sslip.io`, а последующая verify-проверка осталась зелёной.
- [2026-04-06 04:44:23] Создан `docs/REGRESSION_CHECKLIST.md` как канонический маршрут обязательных staging-сценариев перед новыми багфиксами и рефакторингом.
- [2026-04-06 04:48:57] Выполнена внешняя клиентская проверка актуальной staging-ноды: web-sub страница, `clashmeta/first` и endpoint `sub2sing-box` успешно открываются снаружи по `HTTPS`.
- [2026-04-06 04:48:57] Подтверждено, что выдаваемый `clash`-конфиг уже содержит локальные URL форка для probe/rule-provider ресурсов и не указывает на старые внешние raw-источники.
- [2026-04-06 04:48:57] Во время клиентской проверки выявлен остаточный внешний хвост: встроенный web-интерфейс `sub2sing-box` по-прежнему тянет frontend-ресурс с `unpkg.com`; это зафиксировано как следующий целевой багфикс по снижению внешних зависимостей.
- [2026-04-06 04:52:02] Детализация client-side audit по `sub2sing-box` показала, что остаточный внешний frontend-хвост шире одного CSS: UI использует `unpkg.com` для `mdui.css` и `mdui.global.js`, а также `fonts.googleapis.com` для `Roboto`, `Noto Sans SC` и `Material Icons`.
- [2026-04-06 05:13:22] В форк добавлен локальный bundle `vendor/lib/sub2sing-box-ui/`: `mdui.css`, `mdui.global.js`, локальные CSS для `Material Icons` / `Material Icons Outlined`, локальные `woff2`-файлы и минимальный локальный шрифтовой пакет для `sub2sing-box` UI.
- [2026-04-06 05:13:22] В `x-ui-pro-updated.sh` `sub2sing-box` proxy-блок `nginx` переведён на runtime-переписывание HTML через `sub_filter`: CDN-ссылки `unpkg.com` и `fonts.googleapis.com`, а также внешние raw JSON-шаблоны `sb-rule-sets` теперь заменяются на локальные URL форка.
- [2026-04-06 05:13:22] В `x-ui-pro-updated.sh` `stage=websub` теперь не только переустанавливает web-sub bundle, но и нормализует `sub2sing-box` proxy-блок в `includes.conf`, валидирует `nginx` и перезагружает его без полного reinstall.
- [2026-04-06 05:13:22] В `x-ui-pro-updated.sh` усилен `stage=verify`: он теперь отдельно проверяет доступность `sub2sing-box` UI через локальный `HTTPS` и валит regression, если в HTML остались `unpkg.com`, `fonts.googleapis.com` или внешние raw URL `sb-rule-sets`.
- [2026-04-06 05:13:22] На staging VPS `stage=websub -verify yes -debug yes -keep_artifacts yes` подтверждён с полным `PASS` уже после локализации `sub2sing-box` UI.
- [2026-04-06 05:13:22] Внешняя client-side проверка подтвердила, что `sub2sing-box` HTML больше не содержит runtime-утечек на `unpkg.com`, `fonts.googleapis.com` и внешние raw JSON-шаблоны, а локальные CSS/JS/JSON по путям `/SuPFrSWtjs/vendor/lib/sub2sing-box-ui/*` и `/SuPFrSWtjs/vendor/sb-rule-sets/*` отвечают `200 OK`.
- [2026-04-06 05:17:31] `x-ui-pro.sh` физически очищен от исторического installer-тела и превращён в короткий совместимый wrapper на `x-ui-pro-updated.sh`; теперь в файле больше нет мёртвого кода после `exec` и скрытого legacy-пути.
- [2026-04-06 05:17:31] `README.md`, `AI_CONTEXT`, `IMPLEMENTED`, `ROADMAP`, `MASTER_PLAN`, `RESUME_POINT`, `REPOSITORY_MAP` и `SERVICES_PLAN` синхронизированы с новой ролью `x-ui-pro.sh` как единого wrapper-entrypoint.
- [2026-04-06 05:28:47] В `x-ui-pro-updated.sh` исправлена идемпотентность `sub2sing-box` proxy-блока: legacy-строка `sub_filter_types text/html;` теперь вычищается из уже установленного `includes.conf`, но больше не вставляется повторно.
- [2026-04-06 05:28:47] На staging VPS повторно подтверждены `stage=websub`, repeated install через `x-ui-pro.sh` и финальный `stage=verify`; warning `duplicate MIME type "text/html"` больше не воспроизводится ни в `nginx -t`, ни в полном install-логе.
- [2026-04-06 05:38:01] В `x-ui-pro-updated.sh` добавлены `PROJECT_REPO_URL`, `PROJECT_SUPPORT_URL`, `PROJECT_DONATE_URL`, placeholder-подстановка для web-sub страниц и verify-проверка forbidden/allowlist внешних URL.
- [2026-04-06 05:38:01] В `sub-3x-ui.html` удалены устаревшие ссылки `gozargah_marzban` / `Gozargah/Marzban#donation`, placeholder `example.com/path/to/template.json` и подключены project links форка через новые placeholders.
- [2026-04-06 05:38:01] В `sub-3x-ui-classical.html` ссылки на `BLUEBL0B/Secret-Sing-Box` обновлены до актуального `legiz-ru/Secret-Sing-Box`.
- [2026-04-06 05:38:01] На staging VPS `stage=websub -verify yes`, repeated install через `x-ui-pro.sh` и внешний smoke-test подтвердили: forbidden upstream-ссылки больше не воспроизводятся, а web-sub и `sub2sing-box` используют только локальные или allowlist-внешние URL.
- [2026-04-06 05:46:14] В `x-ui-pro-updated.sh` введён явный pinning внешних бинарников через `XUI_REPO_SLUG`, `XUI_VERSION`, `SUB2SINGBOX_REPO_SLUG`, `SUB2SINGBOX_VERSION`, `SUB2SINGBOX_ARCH`.
- [2026-04-06 05:46:14] Install-контур `3x-ui` больше не скачивает `x-ui.sh` отдельно с ветки `main`: CLI теперь берётся из того же tarball, что и остальная `3x-ui` payload-часть, поэтому version pin распространяется и на него.
- [2026-04-06 05:46:14] Установка `sub2sing-box` переведена с жёстко вшитого `v0.0.9 linux_amd64` на управляемые переменные версии и архива, сохраняя текущий проверенный default `v0.0.9` / `amd64`.
- [2026-04-06 05:46:14] На staging VPS repeated install подтверждён с явными `XUI_VERSION=v2.8.11`, `SUB2SINGBOX_VERSION=v0.0.9`, `SUB2SINGBOX_ARCH=amd64`; install-лог показывает pinned-версии, а финальный `stage=verify` остаётся полностью зелёным.
- [2026-04-06 08:17:42] Создан `docs/FORK_PASSPORT.md` как единый подробный паспорт форка: в одном документе собраны исходное состояние upstream, логика проведённых изменений, исправленные баги, архитектурные решения, результаты staging-тестов и текущая точка проекта.
- [2026-04-06 08:17:42] `docs/INDEX.md` обновлён под новый канонический документ форка, а `docs/RESUME_POINT.md` и `docs/MASTER_PLAN.md` дополнены ссылкой на него как на главный подробный источник перед следующей волной fork-функций.
- [2026-04-06 20:52:47] В `x-ui-pro-updated.sh` исправлен парсинг `xray x25519`: installer больше не ищет несуществующие `PrivateKey:` / `PublicKey:`, а корректно разбирает реальный вывод Xray вида `Private key:` / `Public key:`.
- [2026-04-06 20:52:47] В `x-ui-pro-updated.sh` добавлена жёсткая валидация `REALITY`-ключей: при пустом или битом `private/public key` installer теперь завершает установку ошибкой, вместо того чтобы тихо создать сломанный `REALITY`-inbound.
- [2026-04-06 20:52:47] `stage=verify` усилен проверкой `REALITY`-inbound в `x-ui.db`: regression теперь фиксируется, если в базе отсутствует валидная пара `private/public key` для `security = reality`.
- [2026-04-06 21:03:30] Для актуального `xray-linux-amd64` из `3x-ui v2.8.11` уточнён реальный формат `x25519`: публичный клиентский `REALITY`-ключ приходит как `Password`, а не как `PublicKey` / `Public key`; parser installer обновлён под этот формат.
- [2026-04-07 00:56:41] На полностью отформатированном staging VPS `185.207.64.40` выполнен новый clean install из ветки `codex/operational-regression` с доменами `fork.aterium.net` и `forking.aterium.net`; установка завершилась успешно без падения на генерации `REALITY`.
- [2026-04-07 00:56:41] На той же ноде `stage=verify -debug yes -keep_artifacts yes` завершился полным `PASS`, включая новую проверку `REALITY inbound contains valid private/public key pair`.
- [2026-04-07 00:56:41] Прямой запрос `stream_settings` из `x-ui.db` после чистой установки подтвердил, что `REALITY`-inbound на порту `8443` содержит непустой `settings.publicKey`, корректный `privateKey`, `serverNames` и набор `shortIds`.
- [2026-04-07 01:12:08] В форк добавлено локальное зеркало release-архивов `vendor/releases/3x-ui/v2.8.11/x-ui-linux-amd64.tar.gz` и `vendor/releases/sub2sing-box/v0.0.9/sub2sing-box_0.0.9_linux_amd64.tar.gz`, а также манифест `vendor/releases/SHA256SUMS`.
- [2026-04-07 01:12:08] В `x-ui-pro-updated.sh` реализован локальный install-путь release-архивов: installer сначала использует `vendor/releases/*` из клона репозитория, валидирует их по `SHA256SUMS`, и только при отсутствии локального архива уходит в upstream fallback.
- [2026-04-07 01:12:08] На чистом staging VPS подтверждён полный install-путь с локальными зеркалированными архивами: install-лог содержит обе строки `Using local mirrored release archive: ...` для `3x-ui` и `sub2sing-box`.
- [2026-04-07 01:12:08] После install с локальными release-архивами `stage=verify` снова завершился полным `PASS`, поэтому основной `amd64` install-контур уже подтверждён без обязательного upstream-download для `3x-ui` и `sub2sing-box`.
- [2026-04-07 01:16:59] Проверенная ветка `codex/operational-regression` fast-forward перенесена в `main` и опубликована в `origin/main`; основной install-источник репозитория теперь совпадает с фактически подтверждённым staging-контуром.
- [2026-04-07 01:16:59] Проведён отдельный аудит `x-ui-pro-old.sh`: установлено, что файл начинает деструктивную очистку ещё до нормальной обработки аргументов и содержит большой объём устаревших внешних зависимостей.
- [2026-04-07 01:16:59] `x-ui-pro-old.sh` переведён в безопасный archive-mode: в начало файла добавлен guard-блок, который печатает предупреждение и завершает работу с кодом `1`, сохраняя историческое тело ниже только для аудита.
- [2026-04-07 01:16:59] Документация дополнена отдельным файлом `docs/LEGACY_AUDIT.md`, а `README.md`, `INDEX.md` и `REPOSITORY_MAP.md` синхронизированы с новым архивным статусом legacy-скрипта.
- [2026-04-07 01:44:24] Блокировка `x-ui-pro-old.sh` подтверждена runtime-проверкой на staging VPS: скрипт вернул `RC=1`, напечатал archive-warning и не затронул активный `x-ui`.
- [2026-04-07 02:25:32] В `x-ui-pro-updated.sh` нормализован активный console-output для `print_execution_plan()`, `verify_existing_installation()` и управляющей логики `main()`: вместо битых mojibake-строк теперь выводятся стабильные понятные сообщения для `dry-run`, `verify` и post-check сценариев.
- [2026-04-07 02:25:32] Исправление внедрено через новые переопределяющие определения функций перед финальным вызовом `main "$@"`, чтобы безопасно переключить runtime-путь без рискованной перепрошивки большого legacy-файла в одном проходе.
- [2026-04-07 02:25:32] На staging VPS после обновления ветки `codex/legacy-audit` повторно подтверждены `stage=verify -debug yes -keep_artifacts yes`, `stage=verify -dry_run yes`, `stage=websub -dry_run yes` и общий `install -dry_run yes`: активный вывод installer уже идёт без битых строк и завершает проверки успешно.
- [2026-04-07 02:37:53] В `x-ui-pro-updated.sh` завершён следующий безопасный срез shell-рефакторинга: дублирующие нижние определения `print_execution_plan()`, `verify_existing_installation()` и `main()` удалены, а верхние определения приведены к каноническому активному поведению.
- [2026-04-07 02:37:53] Рефакторинг уменьшил размер active-path technical debt: вместо двух конкурирующих наборов функций в installer теперь остаётся один runtime-набор для `dry-run`, `verify`, `websub` и обычного install-потока.
- [2026-04-07 02:37:53] На staging VPS после прямой загрузки нового `x-ui-pro-updated.sh` повторно подтверждены `bash -n`, `stage=verify -debug yes -keep_artifacts yes`, `stage=verify -dry_run yes`, `stage=websub -dry_run yes` и `install -dry_run yes`; все маршруты прошли успешно уже без дублирующих функций в файле.
- [2026-04-07 03:53:02] Начат новый архитектурный этап без кодовых изменений installer'а: в `docs/ARCHITECTURE_NEXT.md` зафиксированы baseline-инварианты текущего форка, границы нового профиля `stealth/zero-signature`, сохранение двух доменов и модель интеграции `AmneziaWG` как отдельного модуля.
- [2026-04-07 03:53:02] Для нового этапа формально разделены реализованное и проектируемое: baseline install-контур не объявляется изменённым, а следующая архитектура описывается как отдельный design-stage до начала кода.
- [2026-04-07 04:00:36] Архитектурный design-stage усилен repo-aware и source-aware выводами: в `ARCHITECTURE_NEXT.md` отдельно зафиксированы ограничения `Xray fallback/REALITY`, роль `Docker` как optional-layer для `AWG`, требования к двухдоменной модели и границы изменений относительно текущего baseline.
- [2026-04-07 04:00:36] В `RULES.md` добавлено рабочее требование использовать параллельных агентов в крупных архитектурных анализах и затем сводить результат в единый документ внутри `docs/`.
- [2026-04-07 04:23:02] Выполнен новый большой архитектурный срез без кодовых изменений installer'а: через параллельный анализ, локальные пользовательские `docx`-материалы и официальные источники собран канонический `docs/PLATFORM_BLUEPRINT.md`.
- [2026-04-07 04:23:02] `PLATFORM_BLUEPRINT.md` зафиксировал итоговый продуктовый вердикт: проект развивается как `local-first` платформа со слоями `core`, `transport modules`, `panel providers`, `web/admin layer`.
- [2026-04-07 04:23:02] В blueprint формализована матрица режимов `classic / stealth / AWG optional`, anti-DPI стратегия для `Xray`, local-first supply chain для `AWG`, политика по `Caddy`, целевая структура репозитория и поэтапный план реализации.
- [2026-04-08 05:09:48] В `codex/platform-v2` выполнен первый безопасный code-slice новой платформы: добавлен каркас `core/platform-lib.sh`, а также каталоги `modules/profiles/classic/`, `modules/transports/classic-xray/` и `providers/panels/3x-ui/`.
- [2026-04-08 05:09:48] В `x-ui-pro-updated.sh` введены зарезервированные platform-v2 переключатели `PLATFORM_PROFILE`, `TRANSPORT_PROFILE`, `PANEL_PROVIDER`, `ENABLE_AWG`, но текущее runtime-поведение baseline `classic` не изменено.
- [2026-04-08 05:09:48] Новый слой подключён через мягкий fallback: при запуске из локального клона metadata берётся из файлов `core/modules/providers`, а при запуске одиночного installer-файла `classic` продолжает работать на встроенных дефолтах.
- [2026-04-08 05:09:48] В `parse_args()` добавлены зарезервированные CLI-флаги `-profile`, `-transport_profile`, `-panel_provider`, `-enable_awg`; пока поддерживается только комбинация `classic + classic-xray + 3x-ui`, а `ENABLE_AWG=yes` режется как ещё не реализованный модуль.
- [2026-04-08 05:09:48] Диагностический слой обновлён: `print_runtime_context()`, `print_execution_plan()` и debug-лог теперь показывают активную platform selection summary.
- [2026-04-08 05:09:48] Локально через Git Bash подтверждены `bash -n` для `x-ui-pro-updated.sh` и `core/platform-lib.sh`, а self-check библиотеки подтвердил корректную инициализацию `classic` и защитный отказ на `ENABLE_AWG=yes`.
- [2026-04-08 05:34:52] В `x-ui-pro-updated.sh` выполнен второй безопасный code-slice `platform-v2`: добавлены dispatcher-обёртки `platform_setup_ingress()`, `platform_enable_ingress()`, `platform_install_panel_provider()`, `platform_apply_transport_profile()`, `platform_restart_panel()`, `platform_enable_panel_service()`.
- [2026-04-08 05:34:52] Основной install-flow в `main()` переведён на новый selection-layer: прямые вызовы `setup_nginx`, `enable_nginx_sites`, `install_panel`, `update_xui_db` и прямое управление `x-ui` заменены на profile/provider-aware dispatcher-вызовы.
- [2026-04-08 05:34:52] В metadata panel provider добавлены `PANEL_PROVIDER_PANEL_TITLE`, `PANEL_PROVIDER_SERVICE_NAME`, `PANEL_PROVIDER_CONTROL_BIN`, а `show_details()` теперь использует эти значения вместо жёсткой привязки к строке `X-UI Secure Panel` и бинарнику `x-ui`.
- [2026-04-08 05:34:52] Внутренности рабочего `classic`-ядра сознательно не переписывались: SQL в `update_xui_db()` и `nginx`-template в `setup_nginx()` оставлены как есть, а новый слой добавлен только на orchestration-уровне.
- [2026-04-08 05:34:52] Локально через Git Bash повторно подтверждены `bash -n` для `x-ui-pro-updated.sh` и `core/platform-lib.sh`, а отдельный self-check показал корректную загрузку provider metadata `X-UI Secure Panel|x-ui|x-ui`.
- [2026-04-08 05:58:05] В `codex/platform-v2` выполнен третий безопасный code-slice: runtime-defaults `classic` переведены на selection-layer через `platform_generate_runtime_defaults()` и metadata `PLATFORM_RUNTIME_TOKEN_LENGTH`, `PLATFORM_CREDENTIAL_LENGTH`, `PLATFORM_DYNAMIC_PORT_BASE`, `PLATFORM_DYNAMIC_PORT_SPAN`.
- [2026-04-08 05:58:05] Boundary-конфигурация `classic` теперь считывается из selection-layer в `load_existing_runtime_context()`, `setup_nginx()` и `update_xui_db()`: публичные порты `80/443`, внутренние порты `7443/8443/9443`, tag `inbound-<port>` и target `127.0.0.1:<port>` больше не прибиты к installer'у как голые литералы.
- [2026-04-08 05:58:05] Локальная граница `sub2sing-box -> nginx` тоже переведена на selection-layer: порт `127.0.0.1:8080` теперь задаётся через `PLATFORM_SUB2SINGBOX_BIND_PORT`, используется в `setup_nginx()`, `ensure_sub2singbox_local_ui_proxy()` и `write_sub2singbox_service()`.
- [2026-04-08 05:58:05] Metadata `classic`-профиля и transport-файлов переписаны в корректной UTF-8-кодировке, чтобы platform-v2 не тащил дальше битую русскоязычную строку в базовые описания.
- [2026-04-08 05:58:05] Локально подтверждены `bash -n` для `x-ui-pro-updated.sh` и `core/platform-lib.sh`, self-check metadata `10|10|10000|49152|80|443|8080|7443|8443|9443` и чистый `git diff --check` без дефектов patch-уровня.
- [2026-04-08 06:09:36] В `codex/platform-v2` выполнен четвёртый безопасный code-slice: provider-level defaults `3x-ui` вынесены из жёстко прибитых литералов `update_xui_db()` в metadata panel provider'а.
- [2026-04-08 06:09:36] `providers/panels/3x-ui/provider.env`, built-in metadata в `core/platform-lib.sh` и fallback metadata в `x-ui-pro-updated.sh` теперь содержат единый набор panel-defaults для `3x-ui`: UI/session defaults, Telegram defaults, locale defaults и subscription defaults.
- [2026-04-08 06:09:36] `update_xui_db()` больше не является единственным источником дефолтов панели: `webListen/webDomain/webCertFile/webKeyFile`, `sessionMaxAge/pageSize/remarkModel`, `tg*`, `timeLocation`, `secretEnable`, `sub*`, `datepicker` теперь подставляются из provider metadata с теми же baseline-значениями.
- [2026-04-08 06:09:36] Локально подтверждены `bash -n` для `x-ui-pro-updated.sh` и `core/platform-lib.sh`, self-check metadata `-ieo|@daily|en-US|Europe/Moscow|12|true|gregorian` и чистый `git diff --check` без дефектов patch-уровня.
- [2026-04-08 06:21:05] В `codex/platform-v2` выполнен пятый безопасный code-slice: panel bootstrap/defaults верхнего уровня (`PANEL_PROVIDER_MIN_VERSION`, bootstrap `username/password/port/webBasePath`) вынесены в provider metadata и helper-слой.
- [2026-04-08 06:21:05] `config_after_install()` больше не содержит жёстких литералов `asdfasdf/2096` в active-path: bootstrap-настройка панели теперь идёт через `platform_panel_bootstrap_*()`, а минимальная поддерживаемая версия `3x-ui` — через `platform_panel_min_version()`.
- [2026-04-08 06:21:05] Локально подтверждены `bash -n` для `x-ui-pro-updated.sh` и `core/platform-lib.sh`, self-check metadata `2.3.5|asdfasdf|asdfasdf|2096|asdfasdf` и чистый `git diff --check` без дефектов patch-уровня.
- [2026-04-08 06:32:49] В `codex/platform-v2` открыт первый `stealth-xray` implementation-slice как staged-profile: добавлены metadata-файлы `modules/profiles/stealth/profile.env` и `modules/transports/stealth-xray/profile.env`, а selection-layer теперь знает связку `stealth + stealth-xray`.
- [2026-04-08 06:32:49] Built-in fallback metadata в `x-ui-pro-updated.sh` и `core/platform-lib.sh` синхронизированы с новым профилем: у `classic` и `stealth` появились явные поля `PLATFORM_IMPLEMENTATION_STATE`, `PLATFORM_INGRESS_OWNER`, `TRANSPORT_IMPLEMENTATION_STATE`, `TRANSPORT_STREAM_MODE`, `TRANSPORT_FALLBACK_TARGET`.
- [2026-04-08 06:32:49] В active-path добавлен защитный early-guard: `stealth` уже доступен для selection/dry-run и валидации комбинаций, но реальная runtime-установка пока сознательно блокируется сообщением о staged-профиле до следующего runtime-среза.
- [2026-04-08 06:32:49] Локально подтверждены `bash -n` для `x-ui-pro-updated.sh` и `core/platform-lib.sh`, self-check `classic|ready|nginx-stream|ready|127.0.0.1:9443`, `stealth|planned|xray|planned|disabled|127.0.0.1:7443` и rejection для невалидной комбинации `classic/stealth-xray`.
- [2026-04-07 04:23:02] `docs/INDEX.md`, `docs/ARCHITECTURE_NEXT.md`, `docs/MASTER_PLAN.md`, `docs/ROADMAP.md` и `docs/RESUME_POINT.md` синхронизированы с новым каноническим blueprint-документом и новым ближайшим implementation-slice.
- [2026-04-08 04:30:47] Начат отдельный организационный этап перед большим обновлением платформы: для веток добавлен канонический `docs/BRANCH_MATRIX.md`, а для документации — канонический `docs/DOCS_MATRIX.md`.
- [2026-04-08 04:30:47] В `docs/` введены два новых сводных документа верхнего уровня: `PROJECT_STATE.md` как единый срез текущего технического состояния и `OPERATIONS.md` как единый срез эксплуатационных сценариев.
- [2026-04-08 04:30:47] `docs/INDEX.md` перепривязан на новый канонический слой и теперь делит документы на три роли: канонические, вспомогательные и исторические.
- [2026-04-08 04:30:47] `docs/MASTER_PLAN.md`, `docs/RESUME_POINT.md` и `docs/ROADMAP.md` синхронизированы с новым организационным этапом и больше не ведут к разрозненным точкам входа.
- [2026-04-08 04:30:47] `docs/STAGING_RESET.md` переписан в корректной кодировке UTF-8, чтобы канонический эксплуатационный слой не ссылался на битый русскоязычный текст.
- [2026-04-08 04:55:17] Открыта новая рабочая ветка `codex/platform-v2` от стабильного `main`.
- [2026-04-08 04:55:17] В `codex/platform-v2` явным переносом собран безопасный подготовительный слой: archive-guard для `x-ui-pro-old.sh`, cleanup active-path в `x-ui-pro-updated.sh` и актуальный архитектурно-организационный слой `docs/`.
- [2026-04-08 23:00:27] Для `stealth` закрыт operational parity baseline: на staging VPS `185.207.64.40` подтверждён маршрут `install -> verify -> websub -> backup -> uninstall -> restore -> reset -> reinstall -> verify`.
- [2026-04-08 23:00:27] В `x-ui-pro-updated.sh` profile-aware cleanup/reset усилен для `stealth`: `verify_reset_state()` теперь проверяет все managed stack ports профиля, `remove_reset_residuals()` освобождает локальные stealth-boundary ports, а `enable_nginx_sites_stealth()` очищает `stream-enabled/stream.conf` и stream-модули.
- [2026-04-08 23:00:27] В `backup.sh` restore-контур стал profile-aware для `stealth`: он определяет профиль по `x-ui.db`, проверяет роли `xray:443` и `nginx:7443`, подтверждает отсутствие `nginx stream.conf` и валидирует HTTPS web-sub через корректный публичный `443`.
- [2026-04-08 23:24:11] В `codex/platform-v2` открыт второй stealth transport `stealth-xhttp`: добавлены transport metadata, selection-layer теперь допускает комбинацию `stealth + stealth-xhttp`, а `load_existing_runtime_context()` научился autodetect этого профиля по `x-ui.db`.
- [2026-04-08 23:24:11] Для `stealth-xhttp` реализован отдельный runtime helper `write_transport_inbounds_stealth_xhttp()`: он поднимает public `REALITY` ingress-shield на `443` и отдельный `xhttp` inbound на `unix:/dev/shm/uds2023.sock,0666` без примеси `ws/trojan` блока.
- [2026-04-08 23:24:11] `verify_existing_installation()` расширен transport-specific проверками `stealth-xhttp`: теперь он подтверждает наличие `xhttp` inbound в `x-ui.db`, публикацию `xhttp` path в nginx fallback и наличие unix socket `/dev/shm/uds2023.sock`.
- [2026-04-08 23:24:11] На staging `185.207.64.40` подтверждён реальный install-path `stealth/stealth-xhttp` на доменах `185.207.64.40.cdn-one.org` и `185-207-64-40.cdn-one.org`: install завершился успешно, `-stage verify` вернул полный `PASS`, а повторный `-stage verify` без явной передачи env подтвердил корректный autodetect нового transport-профиля.
- [2026-04-08 23:24:11] В рамках этого же этапа нормализованы branch-specific документы `README.md`, `docs/BRANCH_CONTEXT.md` и `.env.example`: ветка `codex/platform-v2` теперь явно описывает оба runnable stealth transport-пути и больше не содержит битую кодировку в этих точках входа.
- [2026-04-09 00:15:49] Для блока C текущего `Xray/DPI` этапа создан отдельный канонический документ `docs/ANTI_DPI_MATRIX.md`.
- [2026-04-09 00:15:49] В матрице зафиксированы подтверждённые transport-профили `classic-xray`, `stealth-xray`, `stealth-xhttp`, практические сценарии выбора профиля и обязательные runtime/user-facing/long-running проверки.
- [2026-04-09 00:15:49] Канонические документы `INDEX.md`, `DOCS_MATRIX.md`, `MASTER_PLAN.md`, `PROJECT_STATE.md`, `RESUME_POINT.md`, `ROADMAP.md` и `XRAY_DPI_PLAN.md` синхронизированы с новым активным фокусом: следующий практический шаг — живые клиентские и long-running проверки по anti-DPI матрице.
- [2026-04-09 01:10:14] Первый живой client-load тест для блока C зафиксирован в `docs/`: `stealth-xray` выдержал около 10 минут нагрузки `4K`-видео и параллельного браузерного трафика через `v2rayN` без замеченных обрывов.
- [2026-04-09 01:32:44] Симметричный client-load тест для `stealth-xhttp` тоже зафиксирован в `docs/`: профиль выдержал около 10 минут нагрузки `4K`-видео и параллельного браузерного трафика через `v2rayN` без замеченных обрывов.
- [2026-04-09 01:32:44] `stage=acceptance` усилен transport-aware подсказками для ручной клиентской проверки: в manual checklist теперь дополнительно выводятся актуальные subscription URL, transport-specific hint по выбору узла и предупреждение о смене путей после reinstall.
- [2026-04-09 20:50:25] Новый живой отчёт блока C зафиксирован в канонических docs: короткие client-load тесты пройдены, но старые reality-схемы на mobile internet не подтверждены, а `stealth-xhttp` требует отдельного long-running/time-of-day расследования.
- [2026-04-10 00:34:06] `platform-v2`: подготовлен metadata-driven слой для `xhttp` tuning и связанных `sockopt` в `classic-xray` и `stealth-xhttp`; текущие runtime-дефолты не менялись, но следующий `mobile-safe` preset теперь можно вводить без ручной правки SQL-блоков.
- [2026-04-10 08:06:43] `platform-v2`: metadata-driven tuning расширен и на `REALITY` для stealth-профилей, а `stage=acceptance` получил расширенный mobile-triage bundle (`ss -tinp`, `ss -s`, `journalctl`, transport-aware checklist) без изменения текущих runtime-дефолтов.
- [2026-04-10 08:10:51] `platform-v2`: для `stealth-xhttp` включён первый controlled `mobile-safe` preset. Профиль теперь использует более мягкие `xhttp`/`sockopt` параметры для mobile-data теста, не меняя общую stealth-архитектуру.
- [2026-04-10 08:14:46] `staging`: новый `mobile-safe stealth-xhttp` уже применён на `185.207.64.40`, repeated install завершился успешно, strict `verify` подтвердил `PASS`, а `x-ui.db` показывает новые `xhttpSettings/sockopt` значения пресета.
- [2026-04-10 22:10:47] В `XRAY_DPI_PLAN.md` добавлен обязательный подэтап `Block C1 (must-have)`: формально зафиксированы цели «максимальная невидимость для DPI + устойчивость long-running/reconnect + приемлемая скорость» и условия закрытия этого подэтапа до перехода к `AWG`.
- [2026-04-10 22:10:47] Канонические точки исполнения синхронизированы под `Block C1`: обновлены `PROJECT_STATE.md`, `RESUME_POINT.md` и `MASTER_PLAN.md` (добавлен `[ПУНКТ 52 / В РАБОТЕ]`).
- [2026-04-10 22:10:47] Для наведения порядка в docs добавлен канонический процесс `DOCS_WORKFLOW.md`; `INDEX.md` и `DOCS_MATRIX.md` синхронизированы под ежедневный минимальный набор и единый порядок фиксации контекста.
- [2026-04-10 22:10:47] Навигация и слой веток приведены к текущему состоянию: в `BRANCH_MATRIX.md` раздельно зафиксированы `upstream/reality` и `upstream/subscriptions`, а `branch-html` обновлён под активный `Block C1` и актуальный timestamp генерации.
- [2026-04-10 22:10:47] `RESUME_POINT.md` структурирован в формат «актуально / история уточнений», а `MASTER_PLAN.md` дополнен правилом чтения статусов и новым закрытым пунктом по нормализации docs.
