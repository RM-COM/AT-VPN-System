# RESUME_POINT

## Текущая точка продолжения

- [2026-04-05 05:58:19] Завершена первая фаза локализации repo-owned зависимостей внутри форка.
- [2026-04-05 05:58:19] `vendor/` уже встроен в поддерживаемый контур установщика.
- [2026-04-05 05:58:19] `sub-3x-ui.html`, `sub-3x-ui-classical.html`, `clash/*.yaml`, `vendor/sb-rule-sets/*.json` уже переведены на локальные пути форка.
- [2026-04-05 05:58:19] `vendor/rule-assets/` уже содержит зеркала 52 upstream-файлов, которые раньше подтягивались внешне через шаблоны `sing-box`.
- [2026-04-05 05:58:19] `randomfakehtml.sh` уже работает по локальному набору шаблонов и исключает два шаблона с авто-подгрузкой внешних ресурсов.
- [2026-04-05 06:17:58] В `x-ui-pro-updated.sh` встроен первичный debug-mode с `-stage verify`, `-stage websub`, `-debug yes`, `-dry_run yes`, `-verify yes`, `-keep_artifacts yes`.
- [2026-04-05 06:17:58] Legacy-скрипт `x-ui-pro.sh` теперь безопасно передаёт diagnostic/stage-флаги в `x-ui-pro-updated.sh` до выполнения своей очистки.
- [2026-04-05 06:17:58] Исправлены два важных дефекта install-логики: чтение старой `x-ui.db` теперь происходит до очистки, а `REALITY` public key берётся из корректного поля `PublicKey`.
- [2026-04-05 06:56:44] Реальный full smoke-test уже выполнен на staging VPS `185.207.64.40` с `-auto_domain yes`; после установки `-stage verify` проходит полностью.
- [2026-04-05 06:56:44] Дополнительно закрыты ещё четыре практических дефекта: `CRLF`-несовместимость shell-скриптов на Linux, слишком жёсткий QEMU preflight fail, ложное переключение `-verify yes` в `stage=verify`, некорректный `EXIT trap`.
- [2026-04-05 07:01:41] Локальный репозиторий уже переведён на рабочую git-схему: `origin = RM-COM/AT-VPN-System`, `upstream = mozaroc/x-ui-pro`, текущая базовая ветка — `main`.
- [2026-04-05 07:03:16] Отдельный сценарий `-stage websub -verify yes -debug yes` уже повторно прогнан на установленной staging-ноде и завершился успешно с полным набором `PASS`.
- [2026-04-05 07:04:56] Первый базовый commit форка уже опубликован в `origin/main`, поэтому GitHub-репозиторий инициализирован и готов к дальнейшей работе.
- [2026-04-05 07:06:52] Зафиксировано новое системное правило проекта: основной цикл разработки теперь формализован как `Windows -> GitHub -> Linux staging`, а staging должен уметь возвращаться в чистое состояние перед новым полным прогоном.
- [2026-04-05 07:06:52] Сценарии полного удаления и восстановления признаны обязательной частью форка; их нужно сохранять и включать в дальнейшие проверки при рефакторинге.
- [2026-04-05 07:13:07] Подробный главный план следующего этапа теперь зафиксирован в `MASTER_PLAN.md`; это основной документ, от которого дальше ведётся работа.
- [2026-04-05 07:23:54] `README.md` очищен от следов чужого форка и приведён к актуальному состоянию именно `AT-VPN-System`; теперь он описывает только наш репозиторий, наш install/debug workflow и наши поддерживаемые сценарии.
- [2026-04-05 07:23:54] В правила проекта добавлено обязательное завершение каждого логического этапа через `commit + push` в `origin`; это становится частью основного рабочего контура.
- [2026-04-05 07:35:53] В `x-ui-pro-updated.sh` реализован новый `stage=reset`: staging-ноду теперь можно очищать через отдельный защитный reset-поток с `-confirm_reset yes`, pre-reset артефактами и post-check чистого состояния.
- [2026-04-05 07:35:53] Полный uninstall-контур усилен: вместе с `x-ui` и `nginx` теперь удаляются остатки `sub2sing-box`, `web-sub`, `certbot` и каталоги сертификатов.
- [2026-04-05 07:43:17] Дополнительно исправлен runtime-вывод `stage=reset`: новые сообщения reset-сценария переведены в ASCII English, чтобы staging-консоль не показывала битую кириллицу.
- [2026-04-05 07:49:59] Реальный `stage=reset` на staging VPS уже пройден успешно с полным `PASS`.
- [2026-04-05 07:49:59] Следующий clean install после reset выявил реальный `nginx`-баг порядка `load_module`; в код уже внесён фикс через `modules-enabled`, но повторный runtime-прогон install после этого ещё впереди.
- [2026-04-05 08:02:47] Повторный clean install после фикса `nginx`-модулей подтверждён фактическим состоянием сервера: `nginx`, `x-ui`, `sub2sing-box` активны, `x-ui.db` и `/var/www/subpage` на месте, `nginx -t` проходит.
- [2026-04-05 08:02:47] Отдельные `stage=verify` и `stage=websub` после этого clean install также уже повторно завершились с полным `PASS`.

## Что ещё НЕ завершено

- [2026-04-05 05:58:19] Не решён вопрос о зеркалировании `3x-ui` внутрь активов форка.
- [2026-04-05 05:58:19] Не решён вопрос о зеркалировании `sub2sing-box` внутрь активов форка.
- [2026-04-05 05:58:19] Не проведён следующий этап рефакторинга shell-логики по модулям.
- [2026-04-05 05:58:19] Не разобран окончательно `x-ui-pro-old.sh` как legacy-файл.
- [2026-04-05 07:06:52] Ещё не проведён отдельный regression-прогон путей `uninstall`, `backup` и `restore` уже на нашем форке.

## Наиболее логичный следующий шаг

- [2026-04-05 07:03:16] Следующий практический этап — проверить пользовательские URL, `sub2sing-box` и выдачу `clash.yaml` уже не только через локальную verify-проверку, а глазами клиента.
- [2026-04-05 08:02:47] Следующий практический этап — отдельно прогнать `uninstall`, `backup`, `restore` на staging и закрыть operational regression-контур.
- [2026-04-05 07:35:53] После этого нужно отдельно прогнать сценарии `uninstall`, `backup`, `restore`, чтобы сохранить их как поддерживаемую часть форка и закрыть regression-контур.
- [2026-04-05 06:56:44] Затем можно переходить к следующей волне багфиксов и решению судьбы внешних бинарников `3x-ui` и `sub2sing-box`.
